---
phase: 03-real-time-washer-app
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - apps/washer-mobile/app/(job)/active.tsx
  - apps/washer-mobile/app/(photo)/upload.tsx
  - apps/washer-mobile/app/(job)/complete.tsx
  - apps/washer-mobile/src/components/ChecklistItem.tsx
  - apps/washer-mobile/src/components/PhotoUploader.tsx
autonomous: true
requirements:
  - WASH-05
  - WASH-06
  - PHO-01
  - PHO-02
  - PHO-03
  - PHO-04
  - PHO-06
must_haves:
  truths:
    - "Washer can complete per-category service checklist with 80% gate on Complete Job button"
    - "Washer can take before/after photos using native camera and upload via presigned R2 URL with retry"
    - "Washer can skip photo with a reason (soft gate)"
    - "Job completion flow is sequential: after photo -> checklist summary -> confirm -> order completes"
  artifacts:
    - path: "apps/washer-mobile/app/(job)/active.tsx"
      provides: "Active job screen with checklist"
      contains: "ChecklistItem"
    - path: "apps/washer-mobile/app/(photo)/upload.tsx"
      provides: "Photo upload screen (before/after/pickup/return)"
      contains: "PhotoUploader"
    - path: "apps/washer-mobile/app/(job)/complete.tsx"
      provides: "Job completion flow"
      contains: "Mark Job Complete"
    - path: "apps/washer-mobile/src/components/ChecklistItem.tsx"
      provides: "Checklist row component"
      contains: "Pressable"
    - path: "apps/washer-mobile/src/components/PhotoUploader.tsx"
      provides: "Camera + upload + retry component"
      contains: "usePhotoUpload"
  key_links:
    - from: "apps/washer-mobile/app/(job)/complete.tsx"
      to: "apps/api/src/routes/orders/lifecycle.ts"
      via: "PATCH /api/orders/:id/status { status: 'completed' }"
      pattern: "PATCH.*orders.*status.*completed"
    - from: "apps/washer-mobile/src/components/PhotoUploader.tsx"
      to: "apps/washer-mobile/src/hooks/usePhotoUpload.ts"
      via: "takePhoto + compressAndUpload"
      pattern: "usePhotoUpload"
---

<objective>
Washer active job screen (checklist + GPS broadcasting), photo upload screen (before/after/pickup/return with retry), and job completion flow (after photo -> summary -> confirm).

Purpose: These screens handle the entire on-site service experience from arrival to completion, including photo evidence and service verification.
Output: Three screens and two reusable components covering the complete active job lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-real-time-washer-app/03-CONTEXT.md
@.planning/phases/03-real-time-washer-app/03-UI-SPEC.md
@.planning/phases/03-real-time-washer-app/03-01-SUMMARY.md
@.planning/phases/03-real-time-washer-app/03-02-SUMMARY.md

<interfaces>
From apps/washer-mobile/src/hooks/usePhotoUpload.ts (created in Plan 02):
```typescript
export function usePhotoUpload(token: string): {
  takePhoto: () => Promise<string | null>
  compressAndUpload: (uri: string, orderId: string, photoType: 'before' | 'after' | 'pickup' | 'return') => Promise<string | null>
  uploading: boolean
  progress: number
  error: string | null
  setError: (e: string | null) => void
}
```

From apps/washer-mobile/src/hooks/useGpsTracking.ts (created in Plan 02):
```typescript
export function useGpsTracking(): {
  startTracking: (orderId: string) => Promise<void>
  stopTracking: () => Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChecklistItem + PhotoUploader components</name>
  <files>apps/washer-mobile/src/components/ChecklistItem.tsx, apps/washer-mobile/src/components/PhotoUploader.tsx</files>
  <read_first>
    - apps/washer-mobile/src/hooks/usePhotoUpload.ts (photo upload hook API)
    - .planning/phases/03-real-time-washer-app/03-UI-SPEC.md (Screen 4 photo upload + Screen 5 checklist specs)
    - packages/i18n/locales/en.json (washer.checklist and washer.photo keys)
  </read_first>
  <action>
**1. Create `apps/washer-mobile/src/components/ChecklistItem.tsx` (WASH-05, D-11):**

Single checklist row component per UI-SPEC Screen 5:
- Props: `label: string`, `checked: boolean`, `onToggle: () => void`
- Layout: horizontal row with checkbox (44px touch target min) + item text (16px body)
- Checked state: navy checkmark icon, text dims to muted (`#E8E5DF`)
- Unchecked state: empty square outline, full-opacity navy text
- Touch target: entire row is `Pressable` with `minHeight: 44`
- Use `flexDirection: 'row'` (RTL-aware — checkboxes on trailing side in Arabic per UI-SPEC RTL contract point 7)
- Use `marginStart`/`marginEnd`, NOT marginLeft/marginRight

**2. Create `apps/washer-mobile/src/components/PhotoUploader.tsx` (PHO-01-06, D-07, D-08, D-09):**

Reusable photo capture + upload component per UI-SPEC Screen 4:
- Props: `orderId: string`, `photoType: 'before' | 'after' | 'pickup' | 'return'`, `instruction: string`, `bodyText: string`, `token: string`, `onComplete: () => void`, `onSkip: (reason: string) => void`
- Uses `usePhotoUpload` hook from Plan 02.

**States and UI:**

a) **Initial state (no photo taken):**
- Instruction heading (20px, navy)
- Body text (16px, navy)
- "Open Camera" button: full-width, 56px, gold bg, navy text, camera Lucide icon (20px). On press: calls `takePhoto()` from hook which launches native system camera via `expo-image-picker`.

b) **Photo review state (photo taken, not yet uploaded):**
- Thumbnail: full-width, 200px height, 8px border-radius
- "Upload Photo" button: gold, 44px
- "Retake" link: 16px body, navy — resets to initial state

c) **Uploading state:**
- Linear progress bar below button: gold fill (`#C9A84C`) on muted track (`#E8E5DF`)
- Label: "Uploading... X%" / "جارٍ الرفع… X٪" using `washer.photo.uploading` key with `{{progress}}`

d) **Success state:**
- Green checkmark icon (`#16A34A`) + "Photo saved" / "تم حفظ الصورة"
- Calls `onComplete()` after 1s delay

e) **Error/retry state:**
- Warning banner: `#D97706` background, navy text, "Upload failed — tap to retry" / "فشل الرفع — انقر للمحاولة مرة أخرى"
- Tap to retry calls `compressAndUpload` again
- Background retry continues per D-08

f) **Skip option (D-09):**
- "Skip for now" link at bottom: 14px label, `#D97706` (warning) color
- On tap: bottom sheet/modal with two options:
  - "Poor signal — will upload later" / "إشارة ضعيفة — سيتم الرفع لاحقاً"
  - "No photo needed" / "لا حاجة لصورة"
- Confirmation button. On confirm: calls `onSkip(selectedReason)`.
  </action>
  <verify>
    <automated>cd C:/Users/Rashino/Documents/ROFAN && ls apps/washer-mobile/src/components/ChecklistItem.tsx apps/washer-mobile/src/components/PhotoUploader.tsx && grep -c "usePhotoUpload" apps/washer-mobile/src/components/PhotoUploader.tsx && grep -c "minHeight.*44" apps/washer-mobile/src/components/ChecklistItem.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/washer-mobile/src/components/ChecklistItem.tsx exists with 44px min touch target
    - ChecklistItem uses `flexDirection: 'row'` and responds to RTL
    - apps/washer-mobile/src/components/PhotoUploader.tsx exists and uses `usePhotoUpload` hook
    - PhotoUploader has all 5 states: initial, review, uploading, success, error
    - PhotoUploader has skip option with bottom sheet and two reason choices
    - PhotoUploader "Open Camera" button is 56px, gold background
    - All i18n strings use `useTranslation` with `washer.photo.*` and `washer.checklist.*` keys
    - No marginLeft/marginRight in either component
  </acceptance_criteria>
  <done>ChecklistItem with 44px touch target and RTL support. PhotoUploader with camera launch, upload with retry, progress indicator, success/error states, and skip option with reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Active job, photo upload, and job completion screens</name>
  <files>apps/washer-mobile/app/(job)/active.tsx, apps/washer-mobile/app/(photo)/upload.tsx, apps/washer-mobile/app/(job)/complete.tsx</files>
  <read_first>
    - apps/washer-mobile/src/components/ChecklistItem.tsx (from Task 1)
    - apps/washer-mobile/src/components/PhotoUploader.tsx (from Task 1)
    - apps/washer-mobile/src/hooks/useGpsTracking.ts (GPS tracking hook)
    - .planning/phases/03-real-time-washer-app/03-UI-SPEC.md (Screens 4, 5, 6 specs)
  </read_first>
  <action>
**1. Create `apps/washer-mobile/app/(photo)/upload.tsx` (Screen 4):**

Photo upload screen — reusable for all 4 photo types:
- Route params: `orderId`, `photoType` ('before' | 'after' | 'pickup' | 'return'), `serviceType`
- Background: `#F8F7F4` (brand.surface)
- Renders `PhotoUploader` component with appropriate instruction text based on `photoType`:
  - `before` + on_site: "Take a before photo" (`washer.photo.beforeOnSite`)
  - `before` + carpet (pickup): "Photograph carpets for pickup" (`washer.photo.beforeCarpet`)
  - `after` + on_site: "Take an after photo" (`washer.photo.afterOnSite`)
  - `after` + carpet (return): "Photograph returned carpets" (`washer.photo.afterCarpet`)
- On complete: navigate to next screen in flow (active job or completion)
- On skip: navigate to next screen with skip recorded

**2. Create `apps/washer-mobile/app/(job)/active.tsx` (Screen 5, WASH-04, WASH-05, D-05, D-11):**

Active job screen with checklist:
- GPS broadcasting continues (already started in en-route screen — do NOT restart)
- Android Foreground Service notification visible in system tray (handled by expo-task-manager, not in-app UI)
- Background: `#F8F7F4` (brand.surface)

**Status bar at top:** Navy background strip (#1A2744). Contains:
- Elapsed time (monospace, white, 14px label) — calculated from job start timestamp
- Service type (14px label, white)
- "Job Active" badge: `#16A34A` (success) background, white text, 14px label

**Checklist (D-11):**
- Section heading: "Service Checklist" / "قائمة الخدمة" (20px heading, navy)
- Horizontal progress bar below heading: gold fill on muted track. Label: "X of Y items checked" / "X من Y بنود مكتملة"
- Checklist items per service category:
  - Car wash: "Exterior wash", "Interior vacuum", "Window clean", "Tire clean", "Dashboard wipe", "Final inspection" (6 items)
  - Sofa: "Pre-treatment applied", "Surface clean", "Cushion clean", "Final dry check", "Odor treatment (if requested)", "Final inspection" (6 items)
- Each item rendered as `ChecklistItem` component
- State: array of checked booleans, managed via `useState`

**"Complete Job" button (D-12):**
- Sticky at bottom, full-width, 56px, gold bg, navy semibold text
- Disabled state (muted bg) until >= 80% of items checked
- On press (if < 80% checked): show inline warning below button "Please complete at least X more checklist item(s) before finishing." in `#D97706` (warning), 14px label
- On press (if >= 80%): navigate to after photo screen (`(photo)/upload` with photoType 'after')

**3. Create `apps/washer-mobile/app/(job)/complete.tsx` (Screen 6, D-12, WASH-06):**

Sequential completion flow with 3 steps:

**Step 1 — After photo:** Handled by `(photo)/upload.tsx` before navigating here.

**Step 2 — Checklist summary:**
- Scrollable review of checked items (read-only)
- Heading: "Job Summary" (20px heading, navy)
- Each checked item: green checkmark (`#16A34A`) + item text
- Skipped items: warning icon (`#D97706`) + "Skipped" label

**Step 3 — Confirm completion:**
- Centered screen layout
- "Mark Job Complete?" (20px heading, navy)
- Service type + order number (16px body, monospace for order number)
- "Complete Job" button: gold, 56px, full-width
- "Back" link: 14px label, navy — returns to checklist summary
- On "Complete Job" press:
  - Stop GPS tracking: `stopTracking()` from useGpsTracking hook
  - API call: `PATCH /api/orders/${orderId}/status { status: 'completed' }`
  - On success: show completion confirmation screen

**Completion confirmation:**
- "Job Done" / "تمت المهمة" (28px display, gold `#C9A84C`)
- Earnings amount (28px display, monospace, gold)
- "Back to Home" link (16px body, navy) — navigates to `(home)/index`

**i18n:** Use `washer.completion.*` and `washer.checklist.*` keys.
**RTL:** `marginStart`/`marginEnd` throughout. Numeric amounts wrapped in `direction: 'ltr'` View.
  </action>
  <verify>
    <automated>cd C:/Users/Rashino/Documents/ROFAN && ls apps/washer-mobile/app/\(job\)/active.tsx apps/washer-mobile/app/\(photo\)/upload.tsx apps/washer-mobile/app/\(job\)/complete.tsx && grep -c "ChecklistItem" apps/washer-mobile/app/\(job\)/active.tsx && grep -c "PhotoUploader" apps/washer-mobile/app/\(photo\)/upload.tsx && grep -c "stopTracking" apps/washer-mobile/app/\(job\)/complete.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/washer-mobile/app/(photo)/upload.tsx exists and renders PhotoUploader with dynamic instruction text
    - apps/washer-mobile/app/(job)/active.tsx exists with checklist items per service category
    - Active.tsx has 6 car wash items and 6 sofa items (conditionally rendered)
    - Active.tsx has progress bar showing "X of Y items checked"
    - Active.tsx "Complete Job" button is disabled until >= 80% checked
    - Active.tsx shows inline warning when < 80% and button pressed
    - Active.tsx has navy status bar with elapsed time and "Job Active" badge
    - apps/washer-mobile/app/(job)/complete.tsx exists with 3-step flow
    - Complete.tsx calls `stopTracking()` and `PATCH /api/orders/${orderId}/status { status: 'completed' }` on completion
    - Complete.tsx shows "Job Done" + earnings on success
    - All screens use `useTranslation` for text strings
    - No marginLeft/marginRight in any file
  </acceptance_criteria>
  <done>Active job screen with per-category checklist and 80% completion gate. Photo upload screen handling all 4 photo types. Job completion flow with summary, confirmation, GPS stop, and order status transition to completed.</done>
</task>

</tasks>

<verification>
- Checklist renders correct items per service category
- Complete Job button disabled until 80% threshold
- Photo upload launches camera, compresses, uploads with retry
- Skip option works with reason selection
- Completion flow stops GPS and transitions order to completed
</verification>

<success_criteria>
- All 6 photo requirements satisfied (PHO-01 through PHO-06)
- Service checklist per category (WASH-05)
- Job completion triggers order status transition + payout queue (WASH-06)
- GPS stops on completion
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-time-washer-app/03-05-SUMMARY.md`
</output>
