---
phase: 03-real-time-washer-app
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - apps/washer-mobile/app/(job)/alert.tsx
  - apps/washer-mobile/app/(job)/en-route.tsx
autonomous: true
requirements:
  - WASH-02
  - WASH-03
  - RT-01
must_haves:
  truths:
    - "Washer sees full-screen job alert with 30-second countdown ring that auto-declines and sets offline on expiry"
    - "Washer can accept job with haptic feedback and see route preview with ETA"
    - "Washer can launch Google Maps navigation to customer location"
    - "GPS tracking starts after job accept and broadcasts via Socket.io"
  artifacts:
    - path: "apps/washer-mobile/app/(job)/alert.tsx"
      provides: "Full-screen job alert with countdown"
      contains: "CountdownRing"
    - path: "apps/washer-mobile/app/(job)/en-route.tsx"
      provides: "En route screen with map and navigation"
      contains: "MapView"
  key_links:
    - from: "apps/washer-mobile/app/(job)/alert.tsx"
      to: "apps/washer-mobile/src/hooks/useWasherSocket.ts"
      via: "acceptJob/declineJob callbacks"
      pattern: "acceptJob|declineJob"
    - from: "apps/washer-mobile/app/(job)/en-route.tsx"
      to: "apps/washer-mobile/src/hooks/useGpsTracking.ts"
      via: "startTracking on mount"
      pattern: "startTracking"
---

<objective>
Washer job alert (full-screen takeover with 30s countdown) and en route screen (map + navigation + GPS broadcasting).

Purpose: These are the core washer job acceptance and navigation screens — the critical path from job assignment to arrival at customer location.
Output: Two screens: job alert with countdown timer, en route with live map and Google Maps navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-real-time-washer-app/03-CONTEXT.md
@.planning/phases/03-real-time-washer-app/03-UI-SPEC.md
@.planning/phases/03-real-time-washer-app/03-01-SUMMARY.md
@.planning/phases/03-real-time-washer-app/03-02-SUMMARY.md

<interfaces>
From apps/washer-mobile/src/hooks/useWasherSocket.ts (created in Plan 02):
```typescript
export function useWasherSocket(token: string | null, onJobAlert: (alert: JobAlert) => void): {
  acceptJob: (orderId: string) => void
  declineJob: (orderId: string) => void
}
```

From apps/washer-mobile/src/hooks/useGpsTracking.ts (created in Plan 02):
```typescript
export function useGpsTracking(): {
  startTracking: (orderId: string) => Promise<void>
  stopTracking: () => Promise<void>
}
```

From apps/washer-mobile/src/components/CountdownRing.tsx (created in Plan 02):
```typescript
// Props: duration: number, remaining: number, size: number
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Job alert full-screen takeover screen</name>
  <files>apps/washer-mobile/app/(job)/alert.tsx</files>
  <read_first>
    - apps/washer-mobile/src/hooks/useWasherSocket.ts (acceptJob/declineJob API)
    - apps/washer-mobile/src/components/CountdownRing.tsx (countdown component API)
    - .planning/phases/03-real-time-washer-app/03-UI-SPEC.md (Screen 2 spec — read lines 121-128)
    - packages/i18n/locales/en.json (washer.jobAlert keys)
  </read_first>
  <action>
Create `apps/washer-mobile/app/(job)/alert.tsx` — Screen 2 per UI-SPEC (D-01, D-02, D-03):

**Layout:**
- Covers full screen including status bar area. Background: `#1A2744` (brand.navy).
- Use `StatusBar` component with `barStyle="light-content"` and translucent.
- All content centered vertically and horizontally.

**Header:** "New Job" label (14px, white, centered) at top with safe area top padding.

**Countdown ring (120px):**
- Use CountdownRing component from Plan 02.
- State: `remaining` starts at 30, decremented by `setInterval` at 1000ms.
- Center text: remaining number (28px display, semibold, white).
- Arc color: gold (`#C9A84C`) when remaining >= 10, warning (`#D97706`) when < 10.

**Service details block:**
- Service type icon (32px, Lucide React Native icon matching service category) + service name (20px heading, white).
- Company name (16px body, white at 70% opacity).
- Customer address (16px body, white at 70% opacity).
- Estimated distance (14px label, white at 60% opacity).

**Map preview:**
- `<MapView>` from react-native-maps, 180px height, showing customer pin location.
- Navy-tinted overlay at 30% opacity (View with absolute positioning, backgroundColor: `rgba(26, 39, 68, 0.3)`).
- No user interaction — `scrollEnabled={false}`, `zoomEnabled={false}`, `pitchEnabled={false}`, `rotateEnabled={false}`.

**Buttons:** Two full-width buttons stacked with 16px gap:
- Accept: 56px height, gold bg (`#C9A84C`), navy semibold text, "Accept Job" / "قبول المهمة". On press: `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` then `acceptJob(orderId)` then navigate to `(job)/en-route` with order params.
- Decline: 44px height, transparent bg, `#DC2626` (semantic.destructive) border + text, "Decline" / "رفض". On press: `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` then `declineJob(orderId)` then navigate back to home.

**Timer expiry (D-03):**
- When `remaining` hits 0: auto-call `declineJob(orderId)`, call `PATCH /api/washers/status { online: false }`, show toast "Job auto-declined — you're now offline." / "تم رفض المهمة تلقائياً — أنت الآن غير متاح.", navigate to home.
- No confirmation dialog on auto-decline — happens immediately per UI-SPEC.

**Navigation params:** Receive job alert data via route params or React context (orderId, serviceType, companyName, customerAddress, customerLat, customerLng).

**i18n:** Use `useTranslation()` with keys from `washer.jobAlert.*`.

**RTL:** Use `marginStart`/`marginEnd`. Buttons and text layout must work in both LTR and RTL. Map is direction-neutral.

**Cleanup:** Clear the `setInterval` on unmount.
  </action>
  <verify>
    <automated>cd C:/Users/Rashino/Documents/ROFAN && ls apps/washer-mobile/app/\(job\)/alert.tsx && grep -c "CountdownRing" apps/washer-mobile/app/\(job\)/alert.tsx && grep -c "Haptics" apps/washer-mobile/app/\(job\)/alert.tsx && grep -c "setInterval" apps/washer-mobile/app/\(job\)/alert.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/washer-mobile/app/(job)/alert.tsx exists
    - Contains `CountdownRing` component usage
    - Contains `Haptics.notificationAsync` for accept and `Haptics.impactAsync` for decline
    - Contains `setInterval` with 1000ms for countdown
    - Contains auto-decline logic when remaining === 0 that calls `declineJob` and sets washer offline
    - Contains `MapView` with 180px height and interaction disabled (`scrollEnabled={false}`)
    - Background color is `#1A2744`
    - Accept button has `#C9A84C` background and 56px height
    - Decline button has `#DC2626` border and 44px height
    - Uses `useTranslation` for all text strings
    - No marginLeft/marginRight — only marginStart/marginEnd
  </acceptance_criteria>
  <done>Full-screen job alert with 30-second countdown ring, accept/decline with haptics, auto-decline on expiry with offline status, map preview, all bilingual.</done>
</task>

<task type="auto">
  <name>Task 2: En route screen with map, navigation, and GPS broadcasting</name>
  <files>apps/washer-mobile/app/(job)/en-route.tsx</files>
  <read_first>
    - apps/washer-mobile/src/hooks/useGpsTracking.ts (startTracking/stopTracking API)
    - .planning/phases/03-real-time-washer-app/03-UI-SPEC.md (Screen 3 spec — read lines 129-131)
    - packages/i18n/locales/en.json (washer.enRoute keys)
  </read_first>
  <action>
Create `apps/washer-mobile/app/(job)/en-route.tsx` — Screen 3 per UI-SPEC (D-02, WASH-03, RT-01):

**Layout:** Two sections — map (flex: 1, ~60% height) and bottom sheet (persistent, ~40% height).

**Map section (top ~60%):**
- `<MapView>` from react-native-maps filling flex: 1.
- Google Maps provider on Android, Apple Maps on iOS.
- Customer pin: navy fill (`#1A2744`) marker at customer location.
- Washer pin: animated pulsing gold dot at washer's current location. Use `expo-location` `watchPositionAsync` for foreground position display (the background task from useGpsTracking handles the Socket.io broadcast).
- Route polyline: If Google Maps Directions API is not available (deferred ENH-03), draw a straight-line polyline from washer to customer using `<Polyline>` with gold color.
- Map region: auto-fit to show both washer and customer pins using `fitToCoordinates`.

**Bottom sheet (persistent, ~40%):**
- Background: `#F8F7F4` (brand.surface), rounded top corners 16px radius, shadow.
- Inner padding: 24px (lg spacing).

Contents:
a) Customer name + service type (20px heading, navy).
b) ETA countdown (28px display, gold). Calculate using Haversine formula: `etaMinutes = Math.round(distanceMeters / 8.33)`. Show "Arriving in ~X min" / "الوصول خلال ~X دقيقة". If < 2min: "Arriving now" / "يصل الآن".
c) Address (16px body, navy).
d) "Navigate" button: full-width, 44px, gold bg (`#C9A84C`), navy text. On press: open Google Maps via deep-link:
   - Android: `Linking.openURL(\`google.navigation:q=\${customerLat},\${customerLng}&mode=d\`)`
   - iOS: Try `comgooglemaps://?daddr=${lat},${lng}&directionsmode=driving`, fall back to `https://www.google.com/maps/dir/?api=1&destination=${lat},${lng}&travelmode=driving`
e) "I've Arrived" button: full-width, 44px, navy bg (`#1A2744`), white text. On press: transition order to `in_progress` via `PATCH /api/orders/${orderId}/status { status: 'in_progress' }`, then navigate to before photo screen.
f) "Call Customer" icon button: 44px, muted bg (`#E8E5DF`), navy phone icon. On press: `Linking.openURL(\`tel:\${customerPhone}\`)`.

**GPS tracking:**
- On mount: call `startTracking(orderId)` from useGpsTracking hook. This starts background GPS broadcasting.
- On "I've Arrived": do NOT stop tracking — GPS continues during in_progress state.

**i18n:** Use `useTranslation()` with keys from `washer.enRoute.*`.

**RTL:** `marginStart`/`marginEnd` throughout. `flexDirection` rows use RTL-aware helper.

**Navigation params:** Receive order data (orderId, customerLat, customerLng, customerName, customerPhone, serviceType) via route params.
  </action>
  <verify>
    <automated>cd C:/Users/Rashino/Documents/ROFAN && ls apps/washer-mobile/app/\(job\)/en-route.tsx && grep -c "MapView" apps/washer-mobile/app/\(job\)/en-route.tsx && grep -c "startTracking" apps/washer-mobile/app/\(job\)/en-route.tsx && grep -c "google.navigation\|comgooglemaps" apps/washer-mobile/app/\(job\)/en-route.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/washer-mobile/app/(job)/en-route.tsx exists
    - Contains `<MapView>` with customer pin and washer position
    - Contains `startTracking(orderId)` call on mount
    - Contains Google Maps deep-link for both Android (`google.navigation:q=`) and iOS (`comgooglemaps://`)
    - Contains "I've Arrived" button with `PATCH /api/orders/${orderId}/status` call
    - Contains ETA calculation using `distanceMeters / 8.33`
    - Contains `Linking.openURL` for phone call
    - Bottom sheet has `brand.surface` background and 16px top corner radius
    - All text uses `useTranslation` with `washer.enRoute.*` keys
    - No marginLeft/marginRight
  </acceptance_criteria>
  <done>En route screen shows map with washer/customer pins, route preview, live ETA, Google Maps navigation launch, "I've Arrived" button transitioning to in_progress, GPS broadcasting active.</done>
</task>

</tasks>

<verification>
- Job alert countdown ring animates from 30 to 0
- Accept triggers haptic and navigates to en route
- Timer expiry auto-declines and sets offline
- En route map shows washer and customer pins
- Navigate button opens Google Maps
- GPS tracking starts on mount
</verification>

<success_criteria>
- Job alert full-screen takeover with 30s countdown, accept/decline, auto-decline on expiry (WASH-02, ORD-04, D-01, D-03)
- En route map with ETA, Google Maps navigation, call customer (WASH-03, D-02)
- GPS broadcasting active during en route (RT-01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-time-washer-app/03-04-SUMMARY.md`
</output>
