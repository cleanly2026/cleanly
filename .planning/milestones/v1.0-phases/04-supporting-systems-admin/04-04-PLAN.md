---
phase: 04-supporting-systems-admin
plan: 04
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - apps/admin-web/app/[locale]/layout.tsx
  - apps/admin-web/src/components/admin-sidebar.tsx
  - apps/admin-web/src/components/stat-card.tsx
  - apps/admin-web/src/components/status-badge.tsx
  - apps/admin-web/src/components/data-table.tsx
  - apps/admin-web/app/[locale]/page.tsx
  - apps/admin-web/app/[locale]/companies/page.tsx
  - apps/admin-web/app/[locale]/companies/[id]/page.tsx
  - apps/admin-web/src/components/reject-reason-modal.tsx
  - apps/admin-web/src/lib/api.ts
autonomous: true
requirements: [ADM-01, ADM-02]

must_haves:
  truths:
    - "Admin sees sidebar navigation with all 6 sections"
    - "Dashboard shows 4 stat cards with live counts"
    - "Dashboard shows recent orders table"
    - "Admin can view company list filtered by status"
    - "Admin can approve a company from the detail page"
    - "Admin can reject a company with a required reason"
  artifacts:
    - path: "apps/admin-web/src/components/admin-sidebar.tsx"
      provides: "Fixed sidebar with 6 nav items"
      exports: ["AdminSidebar"]
    - path: "apps/admin-web/app/[locale]/page.tsx"
      provides: "Dashboard with stat cards + recent orders"
      contains: "StatCard"
    - path: "apps/admin-web/app/[locale]/companies/[id]/page.tsx"
      provides: "Company detail with approve/reject"
      contains: "Approve Company"
  key_links:
    - from: "admin-sidebar.tsx"
      to: "layout.tsx"
      via: "imported and rendered in layout"
      pattern: "AdminSidebar"
    - from: "page.tsx (dashboard)"
      to: "/api/admin/orders"
      via: "fetch for stats and recent orders"
      pattern: "api/admin"
    - from: "companies/[id]/page.tsx"
      to: "/api/admin/companies/:id/verify and /reject"
      via: "fetch PATCH on button click"
      pattern: "api/admin/companies"
---

<objective>
Build the admin panel sidebar, dashboard, and company review pages. This creates the primary admin-web user experience: a persistent sidebar navigation, dashboard with 4 stat cards and recent orders, and the full company review workflow (list, detail, approve, reject).

Purpose: ADM-01 (company review) and ADM-02 (order visibility) are the most critical admin features. The sidebar + dashboard form the shell for all subsequent admin pages (Plan 05).

Output: Admin panel with sidebar, dashboard, company list, and company detail with approve/reject workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-supporting-systems-admin/04-CONTEXT.md
@.planning/phases/04-supporting-systems-admin/04-UI-SPEC.md
@.planning/phases/04-supporting-systems-admin/04-02-SUMMARY.md

<interfaces>
<!-- Admin API endpoints from Plan 02 -->

GET /api/admin/companies?status=pending|verified|all&page=1&limit=10
  Returns: { companies: Company[], total: number, page: number, limit: number }

GET /api/admin/companies/:id
  Returns: Company with city, services, packages, members

PATCH /api/admin/companies/:id/verify
  Returns: Company (updated with is_verified=true)

PATCH /api/admin/companies/:id/reject
  Body: { reason: string }
  Returns: Company with rejection_reason

GET /api/admin/orders?status=...&page=1&limit=10
  Returns: { orders: Order[], total: number, page: number, limit: number }

<!-- Existing admin-web layout -->
From apps/admin-web/app/[locale]/layout.tsx:
```typescript
// Cairo font, RTL dir, NextIntlClientProvider, bg-brand-surface
// MODIFY: wrap children with sidebar component
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar component + layout integration + shared components + API client</name>
  <files>
    apps/admin-web/src/components/admin-sidebar.tsx,
    apps/admin-web/src/components/stat-card.tsx,
    apps/admin-web/src/components/status-badge.tsx,
    apps/admin-web/src/components/data-table.tsx,
    apps/admin-web/src/lib/api.ts,
    apps/admin-web/app/[locale]/layout.tsx
  </files>
  <read_first>
    - apps/admin-web/app/[locale]/layout.tsx (current layout — will be modified to include sidebar)
    - .planning/phases/04-supporting-systems-admin/04-UI-SPEC.md (sidebar layout specs, colors, spacing, typography)
    - packages/ui/src/ (check what shared components exist to reuse)
    - apps/customer-web/src/lib/ or similar (check for existing API client pattern)
  </read_first>
  <action>
1. Create `apps/admin-web/src/lib/api.ts` — admin API fetch wrapper:
```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001'

export async function adminFetch<T>(path: string, options?: RequestInit): Promise<T> {
  const res = await fetch(`${API_URL}${path}`, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options?.headers,
    },
    credentials: 'include',
  })
  if (!res.ok) {
    const err = await res.json().catch(() => ({ error: res.statusText }))
    throw new Error(err.error ?? `API error ${res.status}`)
  }
  return res.json()
}
```

2. Create `apps/admin-web/src/components/admin-sidebar.tsx` as a client component:
```typescript
'use client'
```
- Fixed left sidebar, 240px width on desktop, off-canvas drawer on mobile (per UI-SPEC)
- Background: `bg-brand-navy` (#1A2744), text: white
- Active item: 4px gold left border (`border-s-4 border-brand-gold`) + semi-transparent gold tint bg
- Nav items with lucide-react icons:
  - LayoutDashboard icon — "Dashboard" — href: `/${locale}/`
  - Building2 icon — "Companies" — href: `/${locale}/companies`
  - ShoppingCart icon — "Orders" — href: `/${locale}/orders`
  - AlertTriangle icon — "Disputes" — href: `/${locale}/disputes`
  - MapPin icon — "Cities / Categories" — href: `/${locale}/cities`
  - ClipboardList icon — "Audit Log" — href: `/${locale}/audit-log`
- Each nav item: 44px min height (touch target), `ps-lg pe-lg` padding, hover: semi-transparent white bg
- Mobile: hamburger button in top-left, opens sidebar as overlay/drawer
- Use `usePathname()` from next/navigation to determine active item
- Use CSS logical properties throughout: `inset-inline-start`, `ps-`, `pe-`, `ms-`, `me-`
- RTL: sidebar moves to right side via `inset-inline-start: 0` (CSS handles this automatically with logical properties)

3. Create `apps/admin-web/src/components/stat-card.tsx`:
```typescript
interface StatCardProps {
  label: string
  value: number | string
  icon: React.ReactNode
}
```
- Display size (28px semibold) for value, Label size (14px) for label text
- Min height: 96px, background: white, rounded-lg, shadow-sm, padding: md (16px)
- Icon in brand.gold color

4. Create `apps/admin-web/src/components/status-badge.tsx`:
```typescript
interface StatusBadgeProps {
  status: 'pending' | 'approved' | 'rejected' | 'open' | 'resolved' | string
  label?: string
}
```
- Variant mapping:
  - pending/open: `bg-semantic-warning/10 text-semantic-warning` — yellow tint
  - approved/resolved/verified: `bg-semantic-success/10 text-semantic-success` — green tint
  - rejected: `bg-semantic-destructive/10 text-semantic-destructive` — red tint
- Label size (14px), rounded-full, inline-flex, `px-sm py-xs`

5. Create `apps/admin-web/src/components/data-table.tsx` — reusable admin data table:
```typescript
interface DataTableProps<T> {
  columns: { key: string; label: string; render?: (row: T) => React.ReactNode }[]
  data: T[]
  onRowClick?: (row: T) => void
  isLoading?: boolean
  emptyMessage?: string
}
```
- Table header: `bg-brand-navy text-white` (per UI-SPEC color contract)
- Row hover: subtle muted bg
- Loading state: 6 skeleton rows using `bg-brand-muted` (#E8E5DF) animation
- Empty state: centered heading + body text per UI-SPEC states contract
- Pagination controls at bottom (page number, next/prev buttons)

6. Update `apps/admin-web/app/[locale]/layout.tsx`:
   - Import AdminSidebar
   - Wrap children in a flex layout: sidebar on start side + main content area
   - Main content: `ms-[240px]` on desktop (0 on mobile), `max-w-7xl mx-auto px-lg`
   - Add responsive classes: on mobile (`md:ms-[240px] ms-0`)
  </action>
  <verify>
    <automated>cd apps/admin-web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin-web/src/components/admin-sidebar.tsx exists and exports AdminSidebar
    - admin-sidebar.tsx contains 6 nav items: Dashboard, Companies, Orders, Disputes, Cities, Audit Log
    - admin-sidebar.tsx uses `bg-brand-navy` or equivalent navy color class
    - admin-sidebar.tsx uses `border-brand-gold` or equivalent gold for active item
    - admin-sidebar.tsx uses `inset-inline-start` for RTL-safe positioning
    - apps/admin-web/src/components/stat-card.tsx exists and exports StatCard
    - apps/admin-web/src/components/status-badge.tsx exists with variant mapping for pending, approved, rejected, open, resolved
    - apps/admin-web/src/components/data-table.tsx exists with loading skeleton and empty state
    - apps/admin-web/app/[locale]/layout.tsx imports and renders AdminSidebar
    - apps/admin-web/src/lib/api.ts exports adminFetch function
  </acceptance_criteria>
  <done>Admin sidebar renders in layout with all 6 nav items. Shared components (StatCard, StatusBadge, DataTable) ready for dashboard and list pages. API client ready.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page + company list + company detail with approve/reject</name>
  <files>
    apps/admin-web/app/[locale]/page.tsx,
    apps/admin-web/app/[locale]/companies/page.tsx,
    apps/admin-web/app/[locale]/companies/[id]/page.tsx,
    apps/admin-web/src/components/reject-reason-modal.tsx
  </files>
  <read_first>
    - apps/admin-web/src/components/stat-card.tsx (just created — use for dashboard)
    - apps/admin-web/src/components/data-table.tsx (just created — use for orders and companies tables)
    - apps/admin-web/src/components/status-badge.tsx (just created — use for company status)
    - apps/admin-web/src/lib/api.ts (just created — use for all API calls)
    - apps/admin-web/app/[locale]/layout.tsx (confirm sidebar is in place, understand page mount point)
    - .planning/phases/04-supporting-systems-admin/04-UI-SPEC.md (dashboard layout, company review page layout, copywriting contract)
  </read_first>
  <action>
1. Create `apps/admin-web/app/[locale]/page.tsx` — Dashboard page:
   - Server component that fetches initial data
   - `const { locale } = await params` (Next.js 16 async params pattern)
   - 4 StatCards in CSS grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-md`
     - "Orders Today" — count from GET /api/admin/orders?status=all (or dedicated stats endpoint) — for now use total count
     - "Active Washers" — hardcoded or fetched (can use prisma count of online washers)
     - "Pending Reviews" — count from GET /api/admin/companies?status=pending
     - "Open Disputes" — count from GET /api/admin/disputes?status=open
   - Below stat cards: "Recent Orders" heading (Heading size 20px semibold) + DataTable showing last 10 orders
   - Each order row: order ID (truncated CLN-{first 8}), type, status badge, company name, amount (AED), date
   - Row click navigates to `/[locale]/orders/[id]`
   - Empty state: "No orders yet today. New bookings will appear here." (from UI-SPEC copywriting)
   - Error state: inline error banner with destructive left border per UI-SPEC states contract
   - Wrap page in error boundary via `error.tsx` sibling file

2. Create `apps/admin-web/app/[locale]/error.tsx` — error boundary for dashboard:
```typescript
'use client'
export default function ErrorBoundary({ error, reset }: { error: Error; reset: () => void }) {
  return (
    <div className="flex flex-col items-center justify-center min-h-[300px] gap-md">
      <p className="text-semantic-destructive">Something went wrong loading this page.</p>
      <button onClick={reset} className="px-md py-sm bg-brand-gold text-white rounded-lg font-semibold">
        Refresh
      </button>
    </div>
  )
}
```

3. Create `apps/admin-web/app/[locale]/companies/page.tsx` — Company list:
   - Client component (`'use client'`) for filter interactivity
   - Status filter dropdown: "All", "Pending Review", "Approved", "Rejected"
   - DataTable columns: Company Name (EN), City, Status (StatusBadge), Created Date
   - Row click: navigate to `/[locale]/companies/[id]`
   - Pagination: 10 per page
   - Empty state (when filtered to pending): "No companies pending review." (UI-SPEC)
   - Fetch from GET /api/admin/companies?status={filter}&page={page}&limit=10

4. Create `apps/admin-web/app/[locale]/companies/[id]/page.tsx` — Company detail + approve/reject:
   - `const { locale, id } = await params` (Next.js 16 async params)
   - Back link: `< Back to Companies` (arrow mirrors in RTL)
   - Company name in Display size (28px semibold)
   - StatusBadge showing current status
   - Profile section: name_en, name_ar, cities served, service categories, Stripe Connect status
   - Two action buttons at bottom:
     - "Approve Company" button: `bg-brand-gold text-white font-semibold px-lg py-sm rounded-lg` — calls PATCH /api/admin/companies/:id/verify
     - "Reject with Reason" button: `border border-semantic-destructive text-semantic-destructive px-lg py-sm rounded-lg` — opens RejectReasonModal
   - On approve success: optimistic badge update, toast (use simple alert or custom toast for now)
   - On reject success: modal closes, badge updates to "Rejected"
   - On failure: revert badge, show error banner

5. Create `apps/admin-web/src/components/reject-reason-modal.tsx`:
```typescript
'use client'
interface RejectReasonModalProps {
  isOpen: boolean
  onClose: () => void
  onConfirm: (reason: string) => Promise<void>
  companyName: string
}
```
   - Title: "Reject Company Application" (UI-SPEC)
   - Body: "Provide a reason for rejection. This will be sent to the company admin."
   - Textarea: required, placeholder "Enter rejection reason..."
   - CTA: "Confirm Rejection" in `bg-semantic-destructive text-white`
   - Cancel: outline button, closes modal
   - Disable confirm until reason is at least 5 characters
   - On submit: show spinner on button, call onConfirm, close on success, show error inline on failure
   - Use Dialog component from packages/ui if available, otherwise build with div overlay + portal
  </action>
  <verify>
    <automated>cd apps/admin-web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin-web/app/[locale]/page.tsx exists and renders 4 StatCard components
    - Dashboard page contains text "Orders Today", "Active Washers", "Pending Reviews", "Open Disputes"
    - Dashboard page contains "Recent Orders" heading
    - Dashboard page uses DataTable component for recent orders
    - apps/admin-web/app/[locale]/error.tsx exists with reset button
    - apps/admin-web/app/[locale]/companies/page.tsx exists and fetches from /api/admin/companies
    - companies/page.tsx has status filter with All, Pending, Approved, Rejected options
    - apps/admin-web/app/[locale]/companies/[id]/page.tsx exists with `await params`
    - companies/[id]/page.tsx contains "Approve Company" button text
    - companies/[id]/page.tsx contains "Reject with Reason" button text
    - companies/[id]/page.tsx calls PATCH /api/admin/companies/:id/verify on approve
    - apps/admin-web/src/components/reject-reason-modal.tsx exists with isOpen, onClose, onConfirm props
    - reject-reason-modal.tsx contains "Reject Company Application" title text
    - reject-reason-modal.tsx contains "Confirm Rejection" button text
  </acceptance_criteria>
  <done>Dashboard renders 4 stat cards and recent orders. Company list is filterable. Company detail page supports approve and reject with reason modal. All pages follow UI-SPEC copywriting and layout contracts.</done>
</task>

</tasks>

<verification>
- Admin panel loads with sidebar showing all 6 sections
- Dashboard displays 4 stat cards and recent orders table
- Company list is filterable by status
- Company detail shows approve/reject workflow
- RTL: sidebar appears on right, text aligned right, back arrow mirrors
- Error boundaries catch render failures
</verification>

<success_criteria>
- Sidebar renders in layout with 6 nav items and gold active indicator
- Dashboard shows stat cards + recent orders
- Company list filterable by pending/approved/rejected
- Company detail has approve button (gold) and reject with reason (destructive)
- RejectReasonModal requires reason text before submission
</success_criteria>

<output>
After completion, create `.planning/phases/04-supporting-systems-admin/04-04-SUMMARY.md`
</output>
