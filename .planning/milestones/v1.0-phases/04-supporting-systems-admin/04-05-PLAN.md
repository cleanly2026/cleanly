---
phase: 04-supporting-systems-admin
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-04]
files_modified:
  - apps/admin-web/app/[locale]/disputes/page.tsx
  - apps/admin-web/app/[locale]/disputes/[id]/page.tsx
  - apps/admin-web/src/components/dispute-photo-viewer.tsx
  - apps/admin-web/src/components/refund-modal.tsx
  - apps/admin-web/app/[locale]/orders/page.tsx
  - apps/admin-web/app/[locale]/orders/[id]/page.tsx
  - apps/admin-web/app/[locale]/cities/page.tsx
  - apps/admin-web/app/[locale]/audit-log/page.tsx
autonomous: true
requirements: [ADM-02, ADM-03, ADM-04, ADM-05, ADM-06]

must_haves:
  truths:
    - "Admin can view disputes list and detail with before/after photos"
    - "Admin can issue full or partial refund from dispute view"
    - "Admin can view platform-wide orders list and individual order detail"
    - "Admin can CRUD cities and view service categories"
    - "Admin can view audit log filtered by action type and admin user"
  artifacts:
    - path: "apps/admin-web/app/[locale]/disputes/[id]/page.tsx"
      provides: "Dispute detail with photo viewer and refund action"
      contains: "DisputePhotoViewer"
    - path: "apps/admin-web/src/components/refund-modal.tsx"
      provides: "Full/partial refund modal"
      contains: "Confirm Refund"
    - path: "apps/admin-web/app/[locale]/orders/page.tsx"
      provides: "Platform-wide order list"
      contains: "DataTable"
    - path: "apps/admin-web/app/[locale]/cities/page.tsx"
      provides: "City CRUD + category list"
      contains: "Add City"
    - path: "apps/admin-web/app/[locale]/audit-log/page.tsx"
      provides: "Audit log with filters"
      contains: "Audit Log"
  key_links:
    - from: "disputes/[id]/page.tsx"
      to: "/api/admin/refunds/:disputeId/refund"
      via: "fetch POST on refund confirm"
      pattern: "api/admin/refunds"
    - from: "cities/page.tsx"
      to: "/api/admin/cities"
      via: "fetch GET/POST/PATCH/DELETE"
      pattern: "api/admin/cities"
---

<objective>
Build the remaining admin panel pages: disputes (list + detail with photo viewer + refund modal), orders (list + detail), cities/categories CRUD, and audit log. This completes the admin panel feature set.

Purpose: ADM-03 through ADM-06 round out the admin capabilities needed for private beta operations — dispute resolution with photo evidence, city management, and audit trail visibility.

Output: All 6 admin sidebar sections have functional pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-supporting-systems-admin/04-CONTEXT.md
@.planning/phases/04-supporting-systems-admin/04-UI-SPEC.md
@.planning/phases/04-supporting-systems-admin/04-02-SUMMARY.md
@.planning/phases/04-supporting-systems-admin/04-04-SUMMARY.md

<interfaces>
<!-- Admin API from Plan 02 -->

GET /api/admin/disputes?status=open|resolved&page=1&limit=10
  Returns: { disputes: Dispute[], total, page, limit }
  Dispute includes: order.before_photo_url, order.after_photo_url, customer.phone

GET /api/admin/disputes/:id
  Returns: Dispute with full order (customer, company, washer, items, carpet_details)

PATCH /api/admin/disputes/:id/resolve
  Body: { resolution?: string }

POST /api/admin/refunds/:disputeId/refund
  Body: { type: 'full' | 'partial', amount?: number, reason: string }
  Returns: { refund_id, type, amount }

GET /api/admin/orders?status=...&page=1&limit=10
GET /api/admin/orders/:id

GET /api/admin/cities
POST /api/admin/cities — Body: { name_en, name_ar, country? }
PATCH /api/admin/cities/:id — Body: { name_en?, name_ar?, is_active? }
DELETE /api/admin/cities/:id

GET /api/admin/audit-log?action=...&admin_id=...&page=1&limit=20

<!-- Shared components from Plan 04 -->
DataTable, StatusBadge, StatCard from apps/admin-web/src/components/
adminFetch from apps/admin-web/src/lib/api.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disputes pages (list + detail + photo viewer + refund modal)</name>
  <files>
    apps/admin-web/app/[locale]/disputes/page.tsx,
    apps/admin-web/app/[locale]/disputes/[id]/page.tsx,
    apps/admin-web/src/components/dispute-photo-viewer.tsx,
    apps/admin-web/src/components/refund-modal.tsx
  </files>
  <read_first>
    - apps/admin-web/src/components/data-table.tsx (reuse for dispute list)
    - apps/admin-web/src/components/status-badge.tsx (reuse for dispute status)
    - apps/admin-web/src/lib/api.ts (adminFetch for API calls)
    - apps/admin-web/app/[locale]/companies/[id]/page.tsx (established detail page pattern — follow same structure)
    - .planning/phases/04-supporting-systems-admin/04-UI-SPEC.md (dispute view layout, photo viewer specs, refund modal specs, copywriting)
  </read_first>
  <action>
1. Create `apps/admin-web/app/[locale]/disputes/page.tsx` — Dispute list:
   - Client component (`'use client'`)
   - Page title: "Disputes" (Heading size 20px semibold)
   - Status filter: "All", "Open", "Resolved"
   - DataTable columns: Order ID (CLN-{first 8}), Customer Phone, Reason, Status (StatusBadge), Date
   - StatusBadge mapping: open -> "Open" (warning variant), resolved -> "Resolved" (success variant)
   - Row click: navigate to `/[locale]/disputes/[id]`
   - Pagination: 10 per page
   - Empty state: "No disputes reported. Resolved disputes appear in Orders history." (UI-SPEC)
   - Fetch from GET /api/admin/disputes?status={filter}&page={page}

2. Create `apps/admin-web/src/components/dispute-photo-viewer.tsx`:
```typescript
'use client'
interface DisputePhotoViewerProps {
  beforePhotoUrl: string | null
  afterPhotoUrl: string | null
}
```
   - Side-by-side layout: two panels each 50% width on desktop (`grid grid-cols-1 md:grid-cols-2 gap-md`)
   - Each photo: min height 320px, `object-cover rounded-md`
   - Labels above each: "Before" / "After" in Label size
   - If photo URL is null: show placeholder with camera icon + "Photo unavailable" text in muted color
   - Click photo: open in lightbox — full screen overlay with close (X) button. Use `dialog` element or fixed overlay.
   - On mobile: photos stack vertically (grid-cols-1), each full width
   - Image `<img>` tag uses photo URL directly (stored as full public URL from getPublicUrl) — NO presigned URL generation needed
   - Add `onError` handler on `<img>` to show placeholder if URL fails to load

3. Create `apps/admin-web/src/components/refund-modal.tsx`:
```typescript
'use client'
interface RefundModalProps {
  isOpen: boolean
  onClose: () => void
  onConfirm: (type: 'full' | 'partial', amount: number | null, reason: string) => Promise<void>
  orderTotal: number // in fils
}
```
   - Title: "Issue Refund" (UI-SPEC)
   - Radio group: "Full refund -- AED {orderTotal/100}" / "Partial refund"
   - If partial selected: number input for amount in AED (converted to fils before submit), max = orderTotal. Show real-time remaining balance: "Remaining: AED {(orderTotal - amount) / 100}"
   - Reason textarea: required, min 10 chars. Label: "Reason (required)". Placeholder: "Describe the issue and why a refund is being issued"
   - Warning text: "This action cannot be undone. The refund will be issued within 5-10 business days." — in muted/warning style
   - Confirm button: "Confirm Refund" in `bg-semantic-destructive text-white font-semibold` — disabled until reason >= 10 chars
   - Cancel button: "Keep Dispute Open" in outline style
   - On submit: spinner on confirm button, call onConfirm, close modal on success, show inline error on failure
   - Error text from UI-SPEC: "Refund could not be processed. Check Stripe dashboard and try again."

4. Create `apps/admin-web/app/[locale]/disputes/[id]/page.tsx` — Dispute detail:
   - `const { locale, id } = await params`
   - Fetch dispute from GET /api/admin/disputes/:id
   - Layout per UI-SPEC dispute view contract:
     - Order info summary: customer phone, washer phone, company name, order date, order type, amount
     - DisputePhotoViewer with order.before_photo_url and order.after_photo_url
     - Customer dispute note (if exists)
     - Dispute reason as StatusBadge
   - Two action buttons:
     - "Issue Refund" button: `bg-semantic-destructive text-white` — opens RefundModal
     - "Mark as Resolved" button: outline style — calls PATCH /api/admin/disputes/:id/resolve
   - If dispute is already resolved: show resolution text, hide action buttons
   - On refund confirm: POST /api/admin/refunds/:disputeId/refund with { type, amount (in fils), reason }
   - On success: dispute status updates to resolved, show success message
  </action>
  <verify>
    <automated>cd apps/admin-web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin-web/app/[locale]/disputes/page.tsx exists with status filter and DataTable
    - disputes/page.tsx contains "Disputes" heading and "No disputes reported" empty state text
    - apps/admin-web/app/[locale]/disputes/[id]/page.tsx exists with `await params`
    - disputes/[id]/page.tsx renders DisputePhotoViewer component
    - disputes/[id]/page.tsx contains "Issue Refund" and "Mark as Resolved" button text
    - disputes/[id]/page.tsx calls POST /api/admin/refunds/:disputeId/refund on refund confirm
    - apps/admin-web/src/components/dispute-photo-viewer.tsx exports DisputePhotoViewer
    - dispute-photo-viewer.tsx has `grid grid-cols-1 md:grid-cols-2` or equivalent responsive layout
    - dispute-photo-viewer.tsx has onError handler for failed photo loads
    - apps/admin-web/src/components/refund-modal.tsx exports RefundModal
    - refund-modal.tsx contains radio options for "Full refund" and "Partial refund"
    - refund-modal.tsx contains "Confirm Refund" button text
    - refund-modal.tsx contains "This action cannot be undone" warning text
    - refund-modal.tsx validates reason >= 10 characters before enabling confirm button
  </acceptance_criteria>
  <done>Dispute list, detail with photo viewer, and refund modal are functional. Admin can view dispute photos side-by-side, issue full/partial refunds, and mark disputes resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Orders pages + cities/categories CRUD + audit log</name>
  <files>
    apps/admin-web/app/[locale]/orders/page.tsx,
    apps/admin-web/app/[locale]/orders/[id]/page.tsx,
    apps/admin-web/app/[locale]/cities/page.tsx,
    apps/admin-web/app/[locale]/audit-log/page.tsx
  </files>
  <read_first>
    - apps/admin-web/src/components/data-table.tsx (reuse for all three list pages)
    - apps/admin-web/src/components/status-badge.tsx (reuse for order status)
    - apps/admin-web/src/lib/api.ts (adminFetch)
    - apps/admin-web/app/[locale]/companies/page.tsx (established list page pattern)
    - .planning/phases/04-supporting-systems-admin/04-UI-SPEC.md (orders, cities, audit log copywriting and layout)
  </read_first>
  <action>
1. Create `apps/admin-web/app/[locale]/orders/page.tsx` — Order list (ADM-02):
   - Client component
   - Page title: "Orders" (Heading 20px semibold)
   - Status filter dropdown with all OrderStatus values: pending, accepted, washer_assigned, washer_en_route, in_progress, completed, cancelled, plus carpet states
   - DataTable columns: Order # (CLN-{first 8}), Type (on_site/carpet), Status (StatusBadge), Customer Phone, Company Name, Amount (AED), Date
   - Row click: navigate to `/[locale]/orders/[id]`
   - Pagination: 10 per page
   - Fetch from GET /api/admin/orders?status={filter}&page={page}&limit=10

2. Create `apps/admin-web/app/[locale]/orders/[id]/page.tsx` — Order detail (ADM-02):
   - `const { locale, id } = await params`
   - Back link to orders list
   - Order header: Order # (Display size), Type badge, Status badge
   - Info sections:
     - Customer: phone, preferred language
     - Company: name (EN/AR), Stripe status
     - Washer: phone (if assigned)
     - Items: package names, quantities, unit prices
     - Payment: subtotal, platform fee, total (all "AED {amount/100}" format)
     - Carpet details section (if type=carpet): return date, pickup time, carpet count
   - Photo section: before/after photos (if exist) using `<img>` with public URLs
   - Dispute section: if order has dispute, show reason + status + link to dispute detail
   - Read-only view — no edit actions from orders page (admin edits happen via disputes/companies)

3. Create `apps/admin-web/app/[locale]/cities/page.tsx` — City + Category CRUD (ADM-03):
   - Client component
   - Two sections on one page: "Cities" and "Service Categories"
   - Cities section:
     - "Cities" heading + "Add City" button (gold accent)
     - Table: Name (EN), Name (AR), Country, Active (StatusBadge), Actions (Edit/Delete)
     - "Add City" opens inline form (or modal) with: name_en input, name_ar input (`dir="rtl"` on AR input), country select (default AE)
     - Edit: inline editing or modal with same fields
     - Delete: confirmation dialog with text "Delete city: This will hide all companies in this city from discovery. Confirm?" (UI-SPEC)
     - Delete calls DELETE /api/admin/cities/:id (soft delete, sets is_active=false)
     - Empty state: "No cities configured. Add a city to make companies discoverable." (UI-SPEC)
   - Categories section:
     - "Service Categories" heading
     - Read-only list of distinct categories from GET /api/admin/cities/categories
     - Categories are enum-defined (car_wash, carpet, sofa) — display only, no CRUD for enum values
   - All mutations call corresponding /api/admin/cities endpoints

4. Create `apps/admin-web/app/[locale]/audit-log/page.tsx` — Audit Log (ADM-06):
   - Client component
   - Page title: "Audit Log" (Heading 20px semibold)
   - Two filter dropdowns:
     - "Action" filter: All, company.verify, company.reject, dispute.resolve, dispute.refund, city.create, city.update, city.delete
     - "Admin" filter: text input for admin user ID (or dropdown if admin count is small)
   - DataTable columns: Action, Entity, Entity ID, Admin ID, Timestamp
   - Timestamp format: use `Intl.DateTimeFormat` or `date-fns format` for "Apr 5, 2026 at 14:32" style (per UI-SPEC — locale-aware)
   - Pagination: 20 per page (UI-SPEC default)
   - Empty state: "No admin actions recorded yet." (UI-SPEC)
   - Fetch from GET /api/admin/audit-log?action={filter}&admin_id={filter}&page={page}&limit=20
  </action>
  <verify>
    <automated>cd apps/admin-web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin-web/app/[locale]/orders/page.tsx exists with status filter and DataTable
    - orders/page.tsx contains "Orders" heading
    - apps/admin-web/app/[locale]/orders/[id]/page.tsx exists with `await params`
    - orders/[id]/page.tsx displays customer, company, washer, items, payment info
    - orders/[id]/page.tsx displays amounts as "AED" with 2 decimal places
    - apps/admin-web/app/[locale]/cities/page.tsx exists with "Cities" and "Service Categories" sections
    - cities/page.tsx contains "Add City" button
    - cities/page.tsx has delete confirmation text "This will hide all companies"
    - cities/page.tsx calls POST/PATCH/DELETE /api/admin/cities endpoints
    - apps/admin-web/app/[locale]/audit-log/page.tsx exists with action and admin filters
    - audit-log/page.tsx contains "Audit Log" heading
    - audit-log/page.tsx contains "No admin actions recorded yet" empty state
    - audit-log/page.tsx uses pagination with limit=20
  </acceptance_criteria>
  <done>All remaining admin pages built: orders list/detail, cities/categories CRUD, audit log with filters. All 6 admin sidebar sections now have functional pages.</done>
</task>

</tasks>

<verification>
- All 6 sidebar sections have corresponding pages
- Dispute detail shows side-by-side photos
- Refund modal supports both full and partial with validation
- City CRUD works with soft delete
- Audit log filters by action and admin
- All pages handle loading, empty, and error states
</verification>

<success_criteria>
- Dispute list + detail with photo viewer + refund modal functional
- Order list + detail page functional with all order info
- City CRUD with add/edit/delete + confirmation dialogs
- Audit log with action and admin filters, 20 per page pagination
- All pages follow UI-SPEC copywriting, layout, and states contracts
</success_criteria>

<output>
After completion, create `.planning/phases/04-supporting-systems-admin/04-05-SUMMARY.md`
</output>
