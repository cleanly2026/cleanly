---
phase: 01-foundation
plan: "12"
type: execute
wave: 7
depends_on:
  - "01-11"
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - I18N-02
  - I18N-04
  - I18N-05
files_modified: []
must_haves:
  truths:
    - "Customer can sign up with phone OTP, receive a code, and get back a working JWT that persists across app restart"
    - "Washer can log in with phone OTP and 4-digit PIN successfully"
    - "Company admin can log in with email/password and complete TOTP MFA challenge"
    - "Platform admin can log in via Google Workspace SSO only"
    - "Switching the app to Arabic renders the full layout in RTL with Cairo font — no hardcoded English strings visible"
  artifacts:
    - path: "apps/customer-web"
      provides: "Running Next.js customer web with /en/auth and /ar/auth screens"
    - path: "apps/api"
      provides: "Running Fastify API on port 3000 with all auth routes responding"
  key_links:
    - from: "apps/customer-web /ar/auth"
      to: "html[dir=rtl]"
      via: "next-intl locale routing + layout.tsx dir={dir}"
      pattern: "dir.*rtl"
---

<objective>
Human verification of all Phase 1 success criteria. This is the gating checkpoint before Phase 1 is marked complete.

Purpose: All 5 roadmap success criteria for Phase 1 must be verified by a human using the running application. Automated tests cover unit behavior; this checkpoint covers the end-to-end flows that require a browser and Twilio/Google credentials.

Output: Human sign-off that all 5 success criteria pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 foundation:
    - Turborepo monorepo with 6 apps and 6 packages (Plans 01-02)
    - CI/CD pipeline and Sentry scaffolds (Plan 03)
    - Full Prisma schema deployed to Neon PostgreSQL (Plan 04)
    - Fastify API with JWT, Redis, BullMQ (Plan 05)
    - All 4 auth paths: customer OTP, washer OTP+PIN, company email+TOTP, admin Google SSO (Plans 06-07)
    - RTL/i18n infrastructure for web and mobile (Plan 08)
    - Cloudflare R2 scaffold (Plan 09)
    - Custom auth UI components (Plan 10)
    - Auth screens wired to API (Plan 11)
  </what-built>
  <how-to-verify>
Start the services:
```bash
# Terminal 1: Start API
cd apps/api && pnpm dev
# API should be at http://localhost:3000
# Verify: curl http://localhost:3000/health → {"status":"ok","timestamp":"..."}

# Terminal 2: Start customer-web
cd apps/customer-web && pnpm dev
# Customer web should be at http://localhost:3001
```

Verify each Phase 1 success criterion from ROADMAP.md:

**Criterion 1: Customer phone OTP sign-up**
1. Navigate to http://localhost:3001/en/auth
2. Enter a UAE phone number (format: 50 XXX XXXX)
3. Tap "Send Verification Code"
4. Receive SMS on your phone
5. Enter the 6-digit code
6. Tap "Verify Code"
7. ✓ PASS if: you are redirected to home page without errors, and localStorage shows access_token and refresh_token values

**Criterion 2: Washer phone OTP + PIN login**
1. Ensure a washer user exists in the database (use Prisma studio: `pnpm --filter @cleanly/db db:studio`)
2. Test POST /auth/washer/verify with correct phone+OTP+PIN via curl or Insomnia
3. ✓ PASS if: 200 with accessToken and refreshToken
4. Test with wrong PIN: ✓ PASS if: 401 response

**Criterion 3: Company admin email + password + TOTP MFA**
1. Navigate to company admin login (http://localhost:3002 or test via API directly)
2. Enter valid company admin email and password
3. Advance to TOTP challenge screen
4. Enter 6-digit code from Google Authenticator / Authy
5. ✓ PASS if: tokens issued, user is logged in

**Criterion 4: Platform admin Google Workspace SSO**
1. Navigate to http://localhost:3003/en/auth/signin
2. Click "Continue with Google"
3. Complete Google OAuth — try with a @cleanly.ae email
4. ✓ PASS if: redirected to admin dashboard
5. Try again with a non-@cleanly.ae email
6. ✓ PASS if: sign-in is rejected

**Criterion 5: Arabic RTL layout**
1. Navigate to http://localhost:3001/en/auth
2. Click the language toggle (shows "العربية")
3. ✓ PASS if:
   - URL changes to /ar/auth
   - Page layout is RTL (text aligned right, layout mirrored)
   - Cairo font is active (Arabic text renders with correct letterforms)
   - No hardcoded English strings visible — all text is in Arabic
   - `document.documentElement.dir` is "rtl" (check in browser devtools)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass. Or describe which criteria failed so gap closure can be planned.</resume-signal>
</task>

</tasks>

<verification>
All 5 ROADMAP Phase 1 success criteria must be manually verified.
</verification>

<success_criteria>
All 5 Phase 1 success criteria from ROADMAP.md verified as passing:
1. Customer phone OTP → JWT flow works end-to-end
2. Washer OTP + PIN login works, wrong PIN returns 401
3. Company admin email + TOTP MFA completes successfully
4. Platform admin Google SSO restricted to @cleanly.ae domain
5. Arabic RTL layout active with Cairo font, zero hardcoded English
</success_criteria>

<output>
After human approval, create `.planning/phases/01-foundation/01-12-SUMMARY.md` and update ROADMAP.md Phase 1 status to complete.
</output>
