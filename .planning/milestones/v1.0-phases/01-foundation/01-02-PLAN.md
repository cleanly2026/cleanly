---
phase: 01-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/types/src/index.ts
  - packages/types/src/auth.ts
  - packages/types/src/order.ts
  - packages/types/src/api.ts
  - packages/types/tsconfig.json
  - packages/i18n/src/index.ts
  - packages/i18n/locales/en.json
  - packages/i18n/locales/ar.json
  - packages/config/eslint.config.js
  - packages/config/tailwind.config.ts
  - packages/config/tsconfig.base.json
autonomous: true
requirements:
  - INFRA-01
  - I18N-01
  - I18N-03
must_haves:
  truths:
    - "packages/types exports JWTPayload, OrderStatus enum, and VALID_TRANSITIONS — importable from other packages"
    - "packages/i18n/locales/en.json and ar.json exist with all Phase 1 auth keys"
    - "Arabic and English JSON files have identical top-level keys (no key gaps)"
    - "packages/config exports shared ESLint and Tailwind configs"
  artifacts:
    - path: "packages/types/src/auth.ts"
      provides: "JWTPayload interface and role enum"
      exports: ["JWTPayload", "UserRole"]
    - path: "packages/types/src/order.ts"
      provides: "OrderStatus enum and state machine VALID_TRANSITIONS"
      exports: ["OrderStatus", "OrderType", "VALID_TRANSITIONS"]
    - path: "packages/i18n/locales/en.json"
      provides: "All Phase 1 English UI strings"
      contains: "auth.otp.send_cta"
    - path: "packages/i18n/locales/ar.json"
      provides: "All Phase 1 Arabic UI strings"
      contains: "auth.otp.send_cta"
  key_links:
    - from: "packages/types/src/order.ts"
      to: "Prisma schema OrderStatus enum"
      via: "same enum values — must stay in sync"
      pattern: "washer_assigned|pickup_scheduled"
    - from: "packages/i18n/locales/en.json"
      to: "packages/i18n/locales/ar.json"
      via: "identical key structure — CI enforced"
      pattern: "auth\\.otp\\."
---

<objective>
Create the shared TypeScript type package (packages/types) with all cross-package contracts, and the i18n locale files (packages/i18n) with all Phase 1 bilingual strings. Also create shared ESLint and Tailwind config packages.

Purpose: Downstream plans (Prisma schema, auth routes, UI components) import from packages/types for their contracts. The i18n locales establish the zero-hardcoded-strings foundation from day one. These packages have no upstream dependencies — they can be built in wave 2 in parallel with CI setup.

Output: JWTPayload, OrderStatus, VALID_TRANSITIONS exported from packages/types. All Phase 1 auth strings in en.json and ar.json with matching keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build packages/types with auth, order, and API contracts</name>
  <files>
    packages/types/src/auth.ts
    packages/types/src/order.ts
    packages/types/src/api.ts
    packages/types/src/index.ts
    packages/types/tsconfig.json
  </files>
  <read_first>
    - C:\Users\Rashino\Documents\Cleanly\.planning\phases\01-foundation\01-RESEARCH.md (Pattern 4: Fastify JWT Auth Plugin Setup — JWTPayload shape; Pattern 3: Combined Order Status Enum)
    - C:\Users\Rashino\Documents\Cleanly\.planning\phases\01-foundation\01-CONTEXT.md (D-02: exact 14 OrderStatus values)
  </read_first>
  <action>
Create the following files in packages/types/src/:

**packages/types/src/auth.ts**:
```typescript
import { z } from 'zod'

export type UserRole = 'customer' | 'washer' | 'company_member' | 'admin'

export interface JWTPayload {
  sub: string         // userId (cuid)
  role: UserRole
  companyId?: string  // present for company_member only
  iat: number
  exp: number
}

// Zod schema for runtime validation of JWT payload
export const JWTPayloadSchema = z.object({
  sub: z.string().cuid(),
  role: z.enum(['customer', 'washer', 'company_member', 'admin']),
  companyId: z.string().cuid().optional(),
  iat: z.number(),
  exp: z.number(),
})

// OTP request/response schemas
export const SendOtpRequestSchema = z.object({
  phone: z.string().regex(/^971[0-9]{9}$/, 'Phone must be UAE format: 971XXXXXXXXX'),
})
export type SendOtpRequest = z.infer<typeof SendOtpRequestSchema>

export const VerifyOtpRequestSchema = z.object({
  phone: z.string().regex(/^971[0-9]{9}$/),
  code: z.string().length(6).regex(/^\d{6}$/),
})
export type VerifyOtpRequest = z.infer<typeof VerifyOtpRequestSchema>

// Washer PIN schemas
export const WasherPinVerifySchema = z.object({
  phone: z.string().regex(/^971[0-9]{9}$/),
  code: z.string().length(6).regex(/^\d{6}$/),
  pin: z.string().length(4).regex(/^\d{4}$/),
})
export type WasherPinVerify = z.infer<typeof WasherPinVerifySchema>

// Company auth schemas
export const CompanyLoginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
})
export type CompanyLogin = z.infer<typeof CompanyLoginSchema>

export const CompanyTotpVerifySchema = z.object({
  email: z.string().email(),
  token: z.string().length(6).regex(/^\d{6}$/),
  sessionToken: z.string(),  // short-lived pre-MFA token
})
export type CompanyTotpVerify = z.infer<typeof CompanyTotpVerifySchema>

// Token pair response
export interface TokenPair {
  accessToken: string   // 15-minute JWT
  refreshToken: string  // 30-day opaque token
}

export const RefreshTokenSchema = z.object({
  refreshToken: z.string().min(32),
})
```

**packages/types/src/order.ts**:
```typescript
// D-02 (locked): Combined 14-state enum covering both order lifecycles.
// Application layer enforces valid transitions per order type.
export enum OrderStatus {
  // Shared states (both on_site and carpet)
  pending           = 'pending',
  accepted          = 'accepted',
  completed         = 'completed',
  cancelled         = 'cancelled',
  // On-site specific states
  washer_assigned   = 'washer_assigned',
  washer_en_route   = 'washer_en_route',
  in_progress       = 'in_progress',
  // Carpet specific states
  pickup_scheduled  = 'pickup_scheduled',
  picked_up         = 'picked_up',
  in_cleaning       = 'in_cleaning',
  ready_for_return  = 'ready_for_return',
  return_scheduled  = 'return_scheduled',
  out_for_return    = 'out_for_return',
  returned          = 'returned',
}

export type OrderType = 'on_site' | 'carpet'

// State machine: valid transitions per order type.
// Key: current status. Value: allowed next statuses.
export const VALID_TRANSITIONS: Record<OrderType, Partial<Record<OrderStatus, OrderStatus[]>>> = {
  on_site: {
    [OrderStatus.pending]:          [OrderStatus.accepted,       OrderStatus.cancelled],
    [OrderStatus.accepted]:         [OrderStatus.washer_assigned, OrderStatus.cancelled],
    [OrderStatus.washer_assigned]:  [OrderStatus.washer_en_route, OrderStatus.cancelled],
    [OrderStatus.washer_en_route]:  [OrderStatus.in_progress],
    [OrderStatus.in_progress]:      [OrderStatus.completed],
  },
  carpet: {
    [OrderStatus.pending]:          [OrderStatus.accepted,        OrderStatus.cancelled],
    [OrderStatus.accepted]:         [OrderStatus.pickup_scheduled, OrderStatus.cancelled],
    [OrderStatus.pickup_scheduled]: [OrderStatus.picked_up,        OrderStatus.cancelled],
    [OrderStatus.picked_up]:        [OrderStatus.in_cleaning],
    [OrderStatus.in_cleaning]:      [OrderStatus.ready_for_return],
    [OrderStatus.ready_for_return]: [OrderStatus.return_scheduled],
    [OrderStatus.return_scheduled]: [OrderStatus.out_for_return],
    [OrderStatus.out_for_return]:   [OrderStatus.returned],
    [OrderStatus.returned]:         [OrderStatus.completed],
  },
}

export function isValidTransition(
  orderType: OrderType,
  from: OrderStatus,
  to: OrderStatus,
): boolean {
  const allowed = VALID_TRANSITIONS[orderType][from]
  return allowed?.includes(to) ?? false
}
```

**packages/types/src/api.ts**:
```typescript
import { z } from 'zod'

// Standard API error response shape
export const ApiErrorSchema = z.object({
  statusCode: z.number(),
  error: z.string(),
  message: z.string(),
})
export type ApiError = z.infer<typeof ApiErrorSchema>

// Pagination
export const PaginationSchema = z.object({
  page: z.coerce.number().min(1).default(1),
  limit: z.coerce.number().min(1).max(100).default(20),
})
export type Pagination = z.infer<typeof PaginationSchema>

// Language preference
export const LanguageSchema = z.enum(['en', 'ar'])
export type Language = z.infer<typeof LanguageSchema>
```

**packages/types/src/index.ts** — barrel export:
```typescript
export * from './auth'
export * from './order'
export * from './api'
```

**packages/types/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src"]
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/Rashino/Documents/Cleanly" && grep -q "JWTPayload" packages/types/src/auth.ts && grep -q "VALID_TRANSITIONS" packages/types/src/order.ts && grep -q "pickup_scheduled" packages/types/src/order.ts && grep -q "washer_assigned" packages/types/src/order.ts && echo "TYPES OK"</automated>
  </verify>
  <acceptance_criteria>
    - packages/types/src/auth.ts exports: JWTPayload, JWTPayloadSchema, SendOtpRequestSchema, VerifyOtpRequestSchema, WasherPinVerifySchema, CompanyLoginSchema, TokenPair
    - packages/types/src/order.ts exports: OrderStatus enum with all 14 values including pickup_scheduled and washer_en_route
    - packages/types/src/order.ts exports: VALID_TRANSITIONS and isValidTransition function
    - grep "pickup_scheduled" packages/types/src/order.ts returns a match
    - grep "washer_assigned" packages/types/src/order.ts returns a match
    - packages/types/src/index.ts re-exports all three modules
  </acceptance_criteria>
  <done>Shared type contracts established. OrderStatus has 14 states. VALID_TRANSITIONS defines both on_site (7-state) and carpet (10-state) machines.</done>
</task>

<task type="auto">
  <name>Task 2: Create bilingual i18n locale files with all Phase 1 auth strings</name>
  <files>
    packages/i18n/locales/en.json
    packages/i18n/locales/ar.json
    packages/i18n/src/index.ts
    packages/i18n/tsconfig.json
  </files>
  <read_first>
    - C:\Users\Rashino\Documents\Cleanly\.planning\phases\01-foundation\01-UI-SPEC.md (Copywriting Contract section — all i18n keys and English strings; RTL/Bilingual Layout Contract)
    - C:\Users\Rashino\Documents\Cleanly\.planning\REQUIREMENTS.md (I18N-01: zero hardcoded strings; I18N-04: language preference persistence)
  </read_first>
  <action>
Create the locale JSON files covering all Phase 1 auth screen strings. These establish the complete key structure — every subsequent phase adds to these files but NEVER changes existing keys.

**packages/i18n/locales/en.json** — all Phase 1 English strings:
```json
{
  "auth": {
    "otp": {
      "send_cta": "Send Verification Code",
      "verify_cta": "Verify Code",
      "subtitle": "We sent a 6-digit code to {phone}",
      "resend_link": "Resend code",
      "resend_countdown": "Resend in {seconds}s",
      "title": "Enter the 6-digit code",
      "phone_label": "Phone Number",
      "phone_placeholder": "50 XXX XXXX"
    },
    "washer": {
      "pin_title": "Enter your PIN",
      "pin_cta": "Confirm PIN",
      "pin_placeholder": "●"
    },
    "company": {
      "signin_cta": "Sign In",
      "email_label": "Email",
      "password_label": "Password",
      "mfa_title": "Two-Factor Authentication",
      "mfa_subtitle": "Enter the 6-digit code from your authenticator app"
    },
    "admin": {
      "google_cta": "Continue with Google",
      "title": "Admin Sign In",
      "subtitle": "Sign in with your Cleanly Workspace account"
    },
    "empty": {
      "no_session": "Sign in to continue",
      "session_expired": "Your session has expired. Sign in again to access your account."
    },
    "error": {
      "invalid_otp": "Incorrect code. {remaining} attempts remaining.",
      "otp_expired": "Code expired. Tap Resend to get a new code.",
      "rate_limited": "Too many attempts. Try again in {minutes} minutes.",
      "network": "Connection error. Check your internet and try again.",
      "wrong_credentials": "Incorrect email or password.",
      "invalid_totp": "Incorrect code. Try again.",
      "invalid_phone": "Please enter a valid UAE phone number (05X-XXX-XXXX)"
    },
    "terms_copy": "By continuing, you agree to our Terms of Service and Privacy Policy.",
    "lang_toggle": {
      "to_arabic": "العربية",
      "to_english": "English"
    }
  },
  "common": {
    "loading": "Loading...",
    "retry": "Try again",
    "back": "Back",
    "continue": "Continue"
  }
}
```

**packages/i18n/locales/ar.json** — Arabic translations with IDENTICAL key structure:
```json
{
  "auth": {
    "otp": {
      "send_cta": "إرسال رمز التحقق",
      "verify_cta": "تحقق من الرمز",
      "subtitle": "لقد أرسلنا رمزاً مكوناً من 6 أرقام إلى {phone}",
      "resend_link": "إعادة إرسال الرمز",
      "resend_countdown": "إعادة الإرسال خلال {seconds} ثانية",
      "title": "أدخل الرمز المكون من 6 أرقام",
      "phone_label": "رقم الهاتف",
      "phone_placeholder": "50 XXX XXXX"
    },
    "washer": {
      "pin_title": "أدخل رمز PIN الخاص بك",
      "pin_cta": "تأكيد رمز PIN",
      "pin_placeholder": "●"
    },
    "company": {
      "signin_cta": "تسجيل الدخول",
      "email_label": "البريد الإلكتروني",
      "password_label": "كلمة المرور",
      "mfa_title": "التحقق بخطوتين",
      "mfa_subtitle": "أدخل الرمز المكون من 6 أرقام من تطبيق المصادقة الخاص بك"
    },
    "admin": {
      "google_cta": "المتابعة مع Google",
      "title": "تسجيل دخول المشرف",
      "subtitle": "سجل الدخول باستخدام حساب Cleanly Workspace الخاص بك"
    },
    "empty": {
      "no_session": "سجل الدخول للمتابعة",
      "session_expired": "انتهت صلاحية جلستك. سجل الدخول مرة أخرى للوصول إلى حسابك."
    },
    "error": {
      "invalid_otp": "الرمز غير صحيح. {remaining} محاولات متبقية.",
      "otp_expired": "انتهت صلاحية الرمز. اضغط على إعادة الإرسال للحصول على رمز جديد.",
      "rate_limited": "محاولات كثيرة. حاول مرة أخرى خلال {minutes} دقيقة.",
      "network": "خطأ في الاتصال. تحقق من الإنترنت وحاول مرة أخرى.",
      "wrong_credentials": "البريد الإلكتروني أو كلمة المرور غير صحيحة.",
      "invalid_totp": "الرمز غير صحيح. حاول مرة أخرى.",
      "invalid_phone": "الرجاء إدخال رقم هاتف إماراتي صحيح (05X-XXX-XXXX)"
    },
    "terms_copy": "بالمتابعة، أنت توافق على شروط الخدمة وسياسة الخصوصية.",
    "lang_toggle": {
      "to_arabic": "العربية",
      "to_english": "English"
    }
  },
  "common": {
    "loading": "جارٍ التحميل...",
    "retry": "حاول مرة أخرى",
    "back": "رجوع",
    "continue": "متابعة"
  }
}
```

**packages/i18n/src/index.ts** — type-safe locale accessor:
```typescript
import en from '../locales/en.json'
import ar from '../locales/ar.json'

export type Locale = 'en' | 'ar'
export type Messages = typeof en

export const locales = { en, ar } as const satisfies Record<Locale, Messages>

export function getMessages(locale: Locale): Messages {
  return locales[locale]
}
```

**packages/i18n/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "resolveJsonModule": true
  },
  "include": ["src", "locales"]
}
```

Also create **packages/config/eslint.config.js** (shared ESLint config):
```javascript
// @ts-check
import tsPlugin from '@typescript-eslint/eslint-plugin'
import tsParser from '@typescript-eslint/parser'

/** @type {import('eslint').Linter.Config[]} */
export default [
  {
    files: ['**/*.{ts,tsx}'],
    plugins: { '@typescript-eslint': tsPlugin },
    languageOptions: { parser: tsParser, parserOptions: { ecmaVersion: 2022 } },
    rules: {
      // No hardcoded strings rule — enforced by i18n convention
      'no-restricted-syntax': [
        'error',
        {
          selector: 'JSXText[value=/[a-zA-Z]{3,}/]',
          message: 'Hardcoded text in JSX. Use i18n keys from @cleanly/i18n instead.',
        },
      ],
    },
  },
]
```

Also create **packages/config/tailwind.config.ts** (shared Tailwind config consumed by ui package):
```typescript
import type { Config } from 'tailwindcss'

// Per UI-SPEC: brand colors, Cairo font variable, logical property classes
export const sharedTailwindConfig = {
  content: [],
  theme: {
    extend: {
      colors: {
        brand: {
          surface:     '#F8F7F4',  // dominant 60% — page backgrounds
          navy:        '#1A2744',  // secondary 30% — card backgrounds, nav
          gold:        '#C9A84C',  // accent 10% — CTAs, focus rings ONLY
          muted:       '#E8E5DF',  // disabled surfaces, skeleton loaders
        },
        semantic: {
          success:     '#16A34A',
          warning:     '#D97706',
          destructive: '#DC2626',
        },
      },
      fontFamily: {
        cairo: ['var(--font-cairo)', 'system-ui', 'sans-serif'],
      },
      fontSize: {
        'label':   ['14px', { lineHeight: '1.4' }],
        'body':    ['16px', { lineHeight: '1.5' }],
        'heading': ['20px', { lineHeight: '1.3' }],
        'display': ['28px', { lineHeight: '1.2' }],
      },
      fontWeight: {
        regular:  '400',
        semibold: '600',
      },
      spacing: {
        'xs': '4px',
        'sm': '8px',
        'md': '16px',
        'lg': '24px',
        'xl': '32px',
        '2xl': '48px',
        '3xl': '64px',
      },
    },
  },
} satisfies Omit<Config, 'content'>
```
  </action>
  <verify>
    <automated>cd "C:/Users/Rashino/Documents/Cleanly" && grep -q "auth.otp.send_cta" packages/i18n/locales/en.json && grep -q "auth.otp.send_cta" packages/i18n/locales/ar.json && grep -q "إرسال رمز التحقق" packages/i18n/locales/ar.json && echo "I18N OK"</automated>
  </verify>
  <acceptance_criteria>
    - packages/i18n/locales/en.json contains key path auth.otp.send_cta with value "Send Verification Code"
    - packages/i18n/locales/ar.json contains key path auth.otp.send_cta with value "إرسال رمز التحقق"
    - Both JSON files have identical top-level keys: auth and common
    - packages/i18n/locales/ar.json contains auth.error.invalid_otp key
    - packages/config/tailwind.config.ts contains brand.gold: '#C9A84C'
    - packages/config/tailwind.config.ts contains brand.navy: '#1A2744'
    - grep "auth.lang_toggle" packages/i18n/locales/en.json returns a match
    - grep "auth.lang_toggle" packages/i18n/locales/ar.json returns a match
  </acceptance_criteria>
  <done>All Phase 1 bilingual strings exist with matching keys in en.json and ar.json. Tailwind brand tokens declared in shared config.</done>
</task>

</tasks>

<verification>
1. `grep -q "JWTPayload" packages/types/src/auth.ts && echo PASS`
2. `grep -q "pickup_scheduled" packages/types/src/order.ts && echo PASS`
3. `grep -q "isValidTransition" packages/types/src/order.ts && echo PASS`
4. `grep -q "إرسال رمز التحقق" packages/i18n/locales/ar.json && echo PASS`
5. `grep -q "C9A84C" packages/config/tailwind.config.ts && echo PASS`
</verification>

<success_criteria>
- JWTPayload interface defined with sub, role, companyId, iat, exp
- OrderStatus enum has all 14 values including both lifecycle states
- VALID_TRANSITIONS defines legal state machine transitions per order type
- en.json and ar.json both contain all auth.* keys from UI-SPEC Copywriting Contract
- No key gaps between en.json and ar.json
- Brand color tokens declared in shared Tailwind config
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
