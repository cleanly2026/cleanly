# Quick Task 260408-0ki: Fix i18n gap in washer-mobile

## Goal
Replace hardcoded English strings with `t()` i18n calls in washer-mobile screens: photo/upload.tsx, job/active.tsx, and job/alert.tsx. The translation keys already exist in `packages/i18n/locales/en.json` under `washer.photo.*`, `washer.checklist.*`, and `washer.jobAlert.*`.

## Task 1: Replace hardcoded strings with t() calls

**Files:**
- `apps/washer-mobile/app/(photo)/upload.tsx` — INSTRUCTION_MAP and BODY_MAP use hardcoded English
- `apps/washer-mobile/app/(job)/active.tsx` — checklist item labels are hardcoded English
- `apps/washer-mobile/app/(job)/alert.tsx` — serviceType displayed raw (car_wash) instead of translated
- `packages/i18n/locales/en.json` — add any missing translation keys
- `packages/i18n/locales/ar.json` — add Arabic translations for any new keys

**Action:**
1. In `upload.tsx`: Replace `INSTRUCTION_MAP` and `BODY_MAP` hardcoded strings with `t('washer.photo.beforeOnSite')`, `t('washer.photo.afterOnSite')`, etc. Import `useTranslation` if not already imported.
2. In `active.tsx`: Replace hardcoded checklist item labels with `t()` calls. Add checklist item keys to en.json/ar.json if they don't exist (e.g., `washer.checklist.items.car_wash.exterior_wash`, etc.).
3. In `alert.tsx`: Replace raw `serviceType` display with a translated label (e.g., map `car_wash` → `t('services.car_wash')` or similar). Add service type translation keys if missing.
4. Ensure `useTranslation` is imported in all modified files.
5. Add any new translation keys to both `en.json` and `ar.json`.

**Verify:** Metro bundler reloads without errors. Switching language on home screen shows Arabic text on all three screens.

**Done when:** No hardcoded English UI strings remain in photo/upload.tsx, job/active.tsx, or job/alert.tsx.
