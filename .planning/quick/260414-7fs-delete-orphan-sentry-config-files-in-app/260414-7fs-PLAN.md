---
phase: 260414-7fs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-web/sentry.client.config.ts
  - apps/admin-web/sentry.server.config.ts
  - apps/admin-web/sentry.edge.config.ts
autonomous: true
requirements: [QUICK-260414-7fs]
must_haves:
  truths:
    - "apps/admin-web builds with `next build` without a 'Cannot find module @sentry/nextjs' type error"
    - "The three orphan Sentry config files no longer exist in apps/admin-web"
    - "No remaining source/config reference to @sentry/nextjs in apps/admin-web"
  artifacts:
    - path: apps/admin-web/sentry.client.config.ts
      provides: "MUST NOT EXIST — deletion target"
    - path: apps/admin-web/sentry.server.config.ts
      provides: "MUST NOT EXIST — deletion target"
    - path: apps/admin-web/sentry.edge.config.ts
      provides: "MUST NOT EXIST — deletion target"
  key_links:
    - from: "apps/admin-web/next.config.ts"
      to: "(nothing Sentry)"
      via: "absence of withSentryConfig wrapping — confirms orphan status"
      pattern: "withSentryConfig|@sentry/nextjs"
---

<objective>
Unblock the Vercel build for @cleanly/admin-web by deleting three orphan Sentry config files that import `@sentry/nextjs` — a package that is NOT installed and NOT wired into `next.config.ts`. These files are dead code that breaks `next build` type-checking.

Purpose: Restore a green `next build` for admin-web so deployment can proceed.
Output: Three file deletions, one commit, buildable admin-web.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Evidence of orphan status (from discovery)
# 1. apps/admin-web/package.json has NO @sentry/nextjs in dependencies or devDependencies
# 2. apps/admin-web/next.config.ts does NOT wrap with withSentryConfig and has no sentry reference
# 3. Only matches for /sentry/i in apps/admin-web are the three config files + env template entries
# 4. Vercel error: "Cannot find module '@sentry/nextjs'" at sentry.client.config.ts:1

@apps/admin-web/next.config.ts
@apps/admin-web/package.json
@apps/admin-web/sentry.client.config.ts
@apps/admin-web/sentry.server.config.ts
@apps/admin-web/sentry.edge.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the three orphan Sentry config files</name>
  <files>apps/admin-web/sentry.client.config.ts, apps/admin-web/sentry.server.config.ts, apps/admin-web/sentry.edge.config.ts</files>
  <action>
Delete exactly these three files from apps/admin-web:

1. apps/admin-web/sentry.client.config.ts
2. apps/admin-web/sentry.server.config.ts
3. apps/admin-web/sentry.edge.config.ts

Use `git rm` so deletion is staged:

```
git rm apps/admin-web/sentry.client.config.ts apps/admin-web/sentry.server.config.ts apps/admin-web/sentry.edge.config.ts
```

DO NOT install `@sentry/nextjs`. The chosen fix direction (per user) is deletion — Sentry is not in use on admin-web.

DO NOT modify `apps/admin-web/next.config.ts` — it already does not reference Sentry, so no wiring changes are required.

DO NOT touch `apps/admin-web/.env.op.tpl` or `apps/admin-web/.env.example` unless they break the build. They are template files documenting optional env vars; the build failure is purely caused by the three TS files. Leaving the env templates alone is the safe choice per the known_context guidance ("if you're unsure keep the env template entries").

After `git rm`, verify no other code imports `@sentry/nextjs` anywhere in apps/admin-web:

```
grep -R "@sentry/nextjs" apps/admin-web --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" --include="*.json" || echo "OK: no references remain"
```

Expected: "OK: no references remain" (or empty match — grep returns non-zero).
  </action>
  <verify>
    <automated>cd apps/admin-web &amp;&amp; test ! -e sentry.client.config.ts &amp;&amp; test ! -e sentry.server.config.ts &amp;&amp; test ! -e sentry.edge.config.ts &amp;&amp; ! grep -R "@sentry/nextjs" . --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" --exclude-dir=node_modules --exclude-dir=.next</automated>
  </verify>
  <done>
    - All three sentry.*.config.ts files no longer exist in apps/admin-web
    - `git status` shows the three files as deleted (staged via git rm)
    - No remaining `@sentry/nextjs` import anywhere in apps/admin-web source
    - apps/admin-web/next.config.ts is unchanged (no withSentryConfig wrapping was ever present)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify admin-web build no longer fails on Sentry import</name>
  <files>(no files modified — verification only)</files>
  <action>
From the repo root, run the admin-web Next.js build to confirm the Sentry type error is gone:

```
pnpm --filter @cleanly/admin-web build
```

Acceptance:
- The build must NOT fail with: "Cannot find module '@sentry/nextjs' or its corresponding type declarations."
- The build must NOT reference `sentry.client.config.ts`, `sentry.server.config.ts`, or `sentry.edge.config.ts` in any error output.

If the build fails for a DIFFERENT, unrelated reason (pre-existing type error, missing env, etc.), that is OUT OF SCOPE for this quick task — record the new error in the commit body and stop. Do NOT try to fix unrelated errors here; this task's success criterion is strictly "Sentry-orphan error is gone."

If the build succeeds fully, even better — capture the success in the commit body.

Commit the deletions with:

```
git commit -m "fix(admin-web): delete orphan Sentry config files

The three sentry.*.config.ts files imported @sentry/nextjs, which is
not in apps/admin-web/package.json and is not wired into next.config.ts
(no withSentryConfig wrapping). They were dead code breaking Vercel's
next build with 'Cannot find module @sentry/nextjs'.

Deleted:
- apps/admin-web/sentry.client.config.ts
- apps/admin-web/sentry.server.config.ts
- apps/admin-web/sentry.edge.config.ts

Not installing @sentry/nextjs — Sentry is not in use on admin-web."
```
  </action>
  <verify>
    <automated>cd "C:/Users/Rashino/Documents/ROFAN" &amp;&amp; pnpm --filter @cleanly/admin-web build 2>&amp;1 | tee /tmp/admin-web-build.log; ! grep -q "Cannot find module '@sentry/nextjs'" /tmp/admin-web-build.log &amp;&amp; ! grep -q "sentry\\..*\\.config\\.ts" /tmp/admin-web-build.log</automated>
  </verify>
  <done>
    - `pnpm --filter @cleanly/admin-web build` does NOT emit the "@sentry/nextjs" module-not-found error
    - Build log contains no reference to any `sentry.*.config.ts` file
    - Deletion is committed to git with the message above
  </done>
</task>

</tasks>

<verification>
Full-plan sanity checks (run after both tasks):

1. Files gone:
   ```
   ls apps/admin-web/sentry.client.config.ts 2>/dev/null; ls apps/admin-web/sentry.server.config.ts 2>/dev/null; ls apps/admin-web/sentry.edge.config.ts 2>/dev/null
   ```
   Expected: all three commands output nothing (files absent).

2. No lingering references:
   ```
   grep -R "@sentry/nextjs" apps/admin-web --exclude-dir=node_modules --exclude-dir=.next || echo clean
   ```
   Expected: "clean".

3. Git shows deletions committed:
   ```
   git log -1 --stat
   ```
   Expected: most recent commit deletes exactly the three sentry config files.

4. Build no longer crashes on the Sentry import:
   ```
   pnpm --filter @cleanly/admin-web build
   ```
   Expected: no "@sentry/nextjs" error (other unrelated failures are out of scope and must be reported, not fixed).
</verification>

<success_criteria>
Plan is complete when ALL are true:

- [ ] `apps/admin-web/sentry.client.config.ts` does not exist
- [ ] `apps/admin-web/sentry.server.config.ts` does not exist
- [ ] `apps/admin-web/sentry.edge.config.ts` does not exist
- [ ] `grep -R "@sentry/nextjs" apps/admin-web` (excluding node_modules, .next) returns no matches
- [ ] `pnpm --filter @cleanly/admin-web build` no longer errors with "Cannot find module '@sentry/nextjs'"
- [ ] A single commit records the three deletions with a message referencing the Vercel build failure and the orphan status
- [ ] `apps/admin-web/next.config.ts` is unchanged (no additions, no removals)
- [ ] `@sentry/nextjs` is NOT added to any package.json
</success_criteria>

<output>
After completion, create `.planning/quick/260414-7fs-delete-orphan-sentry-config-files-in-app/260414-7fs-SUMMARY.md` summarizing:
- Files deleted (exact paths)
- Build status before vs after
- Commit SHA
- Any unrelated build errors observed (to be triaged as separate quick tasks — not fixed here)
</output>
