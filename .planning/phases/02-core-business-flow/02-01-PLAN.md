---
phase: 02-core-business-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/schema.prisma
  - packages/types/src/order.ts
  - packages/types/src/discovery.ts
  - packages/types/src/booking.ts
  - packages/types/src/company.ts
  - packages/types/src/index.ts
  - packages/i18n/locales/en.json
  - packages/i18n/locales/ar.json
autonomous: true
requirements:
  - DISC-04
  - CARP-03
  - PAY-02
must_haves:
  truths:
    - "Company model has avg_rating and review_count fields for listing display"
    - "Company model has carpet_lead_time_days for return date estimation"
    - "All Phase 2 Zod schemas are exported from @cleanly/types"
    - "All Phase 2 i18n keys exist in both en.json and ar.json"
  artifacts:
    - path: "packages/db/schema.prisma"
      provides: "Schema migration adding avg_rating, review_count, carpet_lead_time_days to Company"
      contains: "avg_rating"
    - path: "packages/types/src/discovery.ts"
      provides: "Zod schemas for discovery API requests/responses"
      exports: ["companyListQuerySchema", "companyProfileResponseSchema"]
    - path: "packages/types/src/booking.ts"
      provides: "Zod schemas for booking API requests/responses"
      exports: ["createOnSiteOrderSchema", "createCarpetOrderSchema"]
    - path: "packages/types/src/company.ts"
      provides: "Zod schemas for company dashboard API requests/responses"
      exports: ["companyProfileSchema", "packageSchema", "washerSchema"]
  key_links:
    - from: "packages/types/src/index.ts"
      to: "packages/types/src/discovery.ts"
      via: "barrel export"
      pattern: "export \\* from './discovery"
---

<objective>
Schema migration and shared type contracts for all Phase 2 features.

Purpose: Every Phase 2 plan depends on shared types (Zod schemas for API validation and client usage), schema fields (avg_rating, review_count, carpet_lead_time_days), and i18n keys. This plan creates all contracts first so downstream plans build against defined interfaces without guessing.

Output: Updated Prisma schema with migration applied, new type files exported from @cleanly/types, complete Phase 2 i18n namespace keys in both languages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-business-flow/02-CONTEXT.md
@.planning/phases/02-core-business-flow/02-RESEARCH.md
@.planning/phases/02-core-business-flow/02-UI-SPEC.md
@packages/db/schema.prisma
@packages/types/src/index.ts
@packages/types/src/order.ts
@packages/i18n/locales/en.json
@packages/i18n/locales/ar.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema migration — add Company fields + apply migration</name>
  <files>packages/db/schema.prisma</files>
  <read_first>
    - packages/db/schema.prisma
    - .planning/phases/02-core-business-flow/02-RESEARCH.md (Open Questions section — fields needed)
  </read_first>
  <action>
Add three new fields to the Company model in `packages/db/schema.prisma`:

```prisma
avg_rating        Decimal   @default(0)   @db.Decimal(3, 2)  // 0.00 to 5.00
review_count      Int       @default(0)
carpet_lead_time_days Int   @default(5)   // days until carpet return, configurable per company
```

Place them after `commission_rate` and before `created_at`.

Then run from monorepo root:
```bash
cd packages/db && pnpm prisma migrate dev --name add_company_rating_and_carpet_lead_time
```

This addresses:
- DISC-04: Company listings need avg_rating and review_count for display (per research Open Question 3)
- CARP-03: Estimated return date needs carpet_lead_time_days (per research Open Question 2)

After migration, run `pnpm prisma generate` to update the client.

Also run the PostGIS postmigrate script if it exists (established Phase 1 pattern):
```bash
pnpm prisma db execute --file prisma/migrations/0002_postgis_indexes.sql 2>/dev/null || true
```
  </action>
  <verify>
    <automated>cd packages/db && pnpm prisma validate && pnpm prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma contains `avg_rating        Decimal   @default(0)`
    - schema.prisma contains `review_count      Int       @default(0)`
    - schema.prisma contains `carpet_lead_time_days Int   @default(5)`
    - A migration file exists under `packages/db/prisma/migrations/` with name containing `add_company_rating`
    - `pnpm prisma validate` exits 0
  </acceptance_criteria>
  <done>Company model has avg_rating, review_count, and carpet_lead_time_days fields. Migration applied. Prisma client regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Shared Zod type contracts + i18n keys for all Phase 2 features</name>
  <files>
    packages/types/src/discovery.ts
    packages/types/src/booking.ts
    packages/types/src/company.ts
    packages/types/src/index.ts
    packages/i18n/locales/en.json
    packages/i18n/locales/ar.json
  </files>
  <read_first>
    - packages/types/src/index.ts
    - packages/types/src/order.ts
    - packages/types/src/auth.ts
    - packages/i18n/locales/en.json
    - packages/i18n/locales/ar.json
    - .planning/phases/02-core-business-flow/02-CONTEXT.md (decisions D-01 through D-20)
    - .planning/phases/02-core-business-flow/02-UI-SPEC.md (Copywriting Contract section)
    - .planning/phases/02-core-business-flow/02-RESEARCH.md (Pattern 1 order creation, Pattern 2 state transition)
  </read_first>
  <action>
**Create `packages/types/src/discovery.ts`:**

```typescript
import { z } from 'zod'

// GET /companies query params
export const companyListQuerySchema = z.object({
  city_id: z.string().min(1),
  category: z.enum(['car_wash', 'carpet', 'sofa']),
  page: z.coerce.number().int().positive().default(1),
})
export type CompanyListQuery = z.infer<typeof companyListQuerySchema>

// GET /cities response item
export const citySchema = z.object({
  id: z.string(),
  name_en: z.string(),
  name_ar: z.string(),
  country: z.string(),
})
export type City = z.infer<typeof citySchema>

// Company listing card data (DISC-04)
export const companyListItemSchema = z.object({
  id: z.string(),
  name_en: z.string(),
  name_ar: z.string(),
  slug: z.string(),
  logo_url: z.string().nullable(),
  avg_rating: z.number(),
  review_count: z.number(),
  starting_price: z.number().int(), // fils — min package price
  city_id: z.string(),
})
export type CompanyListItem = z.infer<typeof companyListItemSchema>

// Company profile (DISC-05)
export const companyProfileResponseSchema = z.object({
  id: z.string(),
  name_en: z.string(),
  name_ar: z.string(),
  description_en: z.string(),
  description_ar: z.string(),
  slug: z.string(),
  logo_url: z.string().nullable(),
  avg_rating: z.number(),
  review_count: z.number(),
  city_id: z.string(),
  carpet_lead_time_days: z.number().int(),
  packages: z.array(z.object({
    id: z.string(),
    category: z.enum(['car_wash', 'carpet', 'sofa']),
    name_en: z.string(),
    name_ar: z.string(),
    description_en: z.string(),
    description_ar: z.string(),
    base_price: z.number().int(),
    add_ons: z.array(z.object({
      id: z.string(),
      name_en: z.string(),
      name_ar: z.string(),
      price: z.number().int(),
    })),
  })),
  reviews: z.array(z.object({
    id: z.string(),
    rating: z.number().int().min(1).max(5),
    comment: z.string().nullable(),
    created_at: z.string(),
  })),
})
export type CompanyProfile = z.infer<typeof companyProfileResponseSchema>
```

**Create `packages/types/src/booking.ts`:**

```typescript
import { z } from 'zod'

// POST /orders — on-site booking (car wash, sofa) per D-05, D-06
export const createOnSiteOrderSchema = z.object({
  package_id: z.string().min(1),
  quantity: z.number().int().positive(),
  add_on_ids: z.array(z.string()).default([]),
  service_location: z.object({
    lat: z.number().min(-90).max(90),
    lng: z.number().min(-180).max(180),
  }),
  location_note: z.string().max(500).optional(),
})
export type CreateOnSiteOrder = z.infer<typeof createOnSiteOrderSchema>

// POST /orders — carpet booking per D-07, D-18
export const createCarpetOrderSchema = z.object({
  package_id: z.string().min(1),
  quantity: z.number().int().positive(), // carpet count
  add_on_ids: z.array(z.string()).default([]),
  pickup_time: z.string().datetime(), // ISO 8601
  location_note: z.string().max(500).optional(),
  service_location: z.object({
    lat: z.number().min(-90).max(90),
    lng: z.number().min(-180).max(180),
  }),
})
export type CreateCarpetOrder = z.infer<typeof createCarpetOrderSchema>

// POST /orders response
export const createOrderResponseSchema = z.object({
  order_id: z.string(),
  client_secret: z.string(), // Stripe PaymentIntent client_secret
})
export type CreateOrderResponse = z.infer<typeof createOrderResponseSchema>

// PATCH /orders/:id/carpet-return-date (CARP-04)
export const rescheduleReturnDateSchema = z.object({
  return_date: z.string().datetime(),
})
export type RescheduleReturnDate = z.infer<typeof rescheduleReturnDateSchema>

// PATCH /orders/:id/status
export const transitionOrderStatusSchema = z.object({
  status: z.string(), // validated against VALID_TRANSITIONS at runtime
})

// PATCH /orders/:id/assign-washer (ORD-03)
export const assignWasherSchema = z.object({
  washer_id: z.string().min(1),
})

// POST /orders/:id/washer-response (ORD-04)
export const washerResponseSchema = z.object({
  accepted: z.boolean(),
})

// Order summary for client display (BOOK-04)
export const orderSummarySchema = z.object({
  order_id: z.string(),
  type: z.enum(['on_site', 'carpet']),
  status: z.string(),
  package_name_en: z.string(),
  package_name_ar: z.string(),
  quantity: z.number().int(),
  amount_subtotal: z.number().int(),
  platform_fee: z.number().int(),
  amount_total: z.number().int(),
  add_ons: z.array(z.object({
    name_en: z.string(),
    name_ar: z.string(),
    price: z.number().int(),
  })),
  created_at: z.string(),
  service_location: z.object({ lat: z.number(), lng: z.number() }).nullable(),
  carpet_details: z.object({
    pickup_time: z.string().nullable(),
    return_date: z.string().nullable(),
    carpet_count: z.number().int().nullable(),
  }).nullable(),
})
export type OrderSummary = z.infer<typeof orderSummarySchema>
```

**Create `packages/types/src/company.ts`:**

```typescript
import { z } from 'zod'

// PUT /company/profile (COMP-01) — bilingual fields required per D-15
export const companyProfileSchema = z.object({
  name_en: z.string().min(2).max(100),
  name_ar: z.string().min(2).max(100),
  description_en: z.string().min(10).max(1000),
  description_ar: z.string().min(10).max(1000),
  logo_url: z.string().url().optional(),
})
export type CompanyProfileInput = z.infer<typeof companyProfileSchema>

// PUT /company/services (COMP-02)
export const companyServicesSchema = z.object({
  city_ids: z.array(z.string().min(1)).min(1),
  categories: z.array(z.enum(['car_wash', 'carpet', 'sofa'])).min(1),
})
export type CompanyServicesInput = z.infer<typeof companyServicesSchema>

// POST /company/packages (COMP-03)
export const packageSchema = z.object({
  category: z.enum(['car_wash', 'carpet', 'sofa']),
  name_en: z.string().min(2).max(100),
  name_ar: z.string().min(2).max(100),
  description_en: z.string().min(5).max(500),
  description_ar: z.string().min(5).max(500),
  base_price: z.number().int().positive(), // in fils
})
export type PackageInput = z.infer<typeof packageSchema>

// POST /company/packages/:id/add-ons (COMP-03)
export const addOnSchema = z.object({
  name_en: z.string().min(2).max(100),
  name_ar: z.string().min(2).max(100),
  price: z.number().int().positive(), // in fils
})
export type AddOnInput = z.infer<typeof addOnSchema>

// POST /company/washers (COMP-04)
export const washerSchema = z.object({
  phone: z.string().min(8).max(15),
  name: z.string().min(2).max(100),
})
export type WasherInput = z.infer<typeof washerSchema>

// Stripe Connect onboarding (COMP-07)
export const stripeConnectResponseSchema = z.object({
  url: z.string().url(), // AccountLink URL
})
export type StripeConnectResponse = z.infer<typeof stripeConnectResponseSchema>
```

**Update `packages/types/src/index.ts`:**

Add these barrel exports:
```typescript
export * from './discovery.js'
export * from './booking.js'
export * from './company.js'
```

**Update `packages/i18n/locales/en.json`:**

Add these Phase 2 namespaces (merge into existing JSON):

```json
{
  "discovery": {
    "categories": {
      "car_wash": "Car Wash",
      "carpet": "Carpet Cleaning",
      "sofa": "Sofa Cleaning"
    },
    "city_badge": "Your city",
    "city_picker_title": "Select your city",
    "no_companies_heading": "No companies yet in {{city}}",
    "no_companies_body": "We're growing our network. Check back soon or try a nearby city.",
    "no_reviews": "No reviews yet — be the first to book and share your experience.",
    "company_listing_starting_from": "From",
    "company_profile_packages": "Packages",
    "company_profile_add_ons": "Add-ons",
    "company_profile_reviews": "Reviews",
    "company_profile_see_all_reviews": "See all reviews",
    "gps_unavailable": "Location access is off. Please enable it in your device settings, or select your city manually."
  },
  "booking": {
    "book_now": "Book Now",
    "confirm_pay": "Confirm & Pay AED {{amount}}",
    "service_fee_label": "Service fee (15%)",
    "order_summary": "Order Summary",
    "subtotal": "Subtotal",
    "total": "Total",
    "location_pin_title": "Pin your location",
    "parking_notes_placeholder": "Parking instructions or access notes",
    "carpet_pickup_date": "Pickup Date",
    "carpet_pickup_time": "Pickup Time Slot",
    "carpet_estimated_return": "Estimated return: {{date}}",
    "carpet_count": "Number of carpets",
    "quantity": "Quantity",
    "payment_failed": "Payment could not be processed. Please check your card details or try a different payment method.",
    "booking_failed": "Something went wrong placing your order. Your card has not been charged. Please try again.",
    "booking_confirmed_heading": "Booking Confirmed",
    "view_order": "View Order"
  },
  "order": {
    "status": {
      "pending": "Pending",
      "accepted": "Accepted",
      "washer_assigned": "Washer Assigned",
      "washer_en_route": "Washer En Route",
      "in_progress": "In Progress",
      "completed": "Completed",
      "cancelled": "Cancelled",
      "pickup_scheduled": "Pickup Scheduled",
      "picked_up": "Picked Up",
      "in_cleaning": "In Cleaning",
      "ready_for_return": "Ready for Return",
      "return_scheduled": "Return Scheduled",
      "out_for_return": "Out for Return",
      "returned": "Returned"
    },
    "cancel_order": "Cancel Order",
    "cancel_confirm_heading": "Cancel your order?",
    "cancel_confirm_body": "Cancellation is free before the washer is assigned. A fee may apply after.",
    "cancel_confirm_cta": "Yes, Cancel Order",
    "order_number": "Order #{{number}}"
  },
  "dashboard": {
    "order_feed_title": "Orders",
    "filter_all": "All",
    "filter_pending": "Pending",
    "filter_active": "Active",
    "filter_completed": "Completed",
    "assign_washer": "Assign Washer",
    "confirm_assignment": "Confirm",
    "no_orders_heading": "No orders yet",
    "no_orders_body": "New bookings will appear here in real time.",
    "save_continue": "Save & Continue",
    "onboarding_profile": "Profile",
    "onboarding_services": "Services",
    "onboarding_packages": "Packages",
    "onboarding_stripe": "Stripe Connect",
    "connect_stripe": "Connect Stripe Account",
    "stripe_connected": "Stripe Connected",
    "add_package": "Add Package",
    "delete_package_confirm": "Delete this package? This cannot be undone.",
    "delete_package_cta": "Delete Package",
    "add_washer": "Add Washer",
    "deactivate_washer_confirm": "Deactivate {{name}}? They will no longer receive new assignments.",
    "deactivate_washer_cta": "Deactivate"
  }
}
```

**Update `packages/i18n/locales/ar.json`:**

Add the Arabic equivalents (merge into existing JSON):

```json
{
  "discovery": {
    "categories": {
      "car_wash": "غسيل سيارات",
      "carpet": "تنظيف سجاد",
      "sofa": "تنظيف كنب"
    },
    "city_badge": "مدينتك",
    "city_picker_title": "اختر مدينتك",
    "no_companies_heading": "لا توجد شركات بعد في {{city}}",
    "no_companies_body": "نحن نوسع شبكتنا. عد قريبًا أو جرب مدينة قريبة.",
    "no_reviews": "لا توجد تقييمات بعد — كن أول من يحجز ويشارك تجربته.",
    "company_listing_starting_from": "ابتداءً من",
    "company_profile_packages": "الباقات",
    "company_profile_add_ons": "الإضافات",
    "company_profile_reviews": "التقييمات",
    "company_profile_see_all_reviews": "عرض جميع التقييمات",
    "gps_unavailable": "خدمة الموقع معطلة. يرجى تفعيلها في إعدادات جهازك أو اختيار مدينتك يدويًا."
  },
  "booking": {
    "book_now": "احجز الآن",
    "confirm_pay": "تأكيد ودفع {{amount}} د.إ",
    "service_fee_label": "رسوم الخدمة (15٪)",
    "order_summary": "ملخص الطلب",
    "subtotal": "المجموع الفرعي",
    "total": "الإجمالي",
    "location_pin_title": "حدد موقعك",
    "parking_notes_placeholder": "تعليمات الوقوف أو ملاحظات الوصول",
    "carpet_pickup_date": "تاريخ الاستلام",
    "carpet_pickup_time": "فترة الاستلام",
    "carpet_estimated_return": "الإرجاع المتوقع: {{date}}",
    "carpet_count": "عدد السجاد",
    "quantity": "الكمية",
    "payment_failed": "تعذرت معالجة الدفع. يرجى التحقق من بيانات بطاقتك أو تجربة طريقة دفع أخرى.",
    "booking_failed": "حدث خطأ أثناء تقديم طلبك. لم يتم خصم أي مبلغ من بطاقتك. يرجى المحاولة مرة أخرى.",
    "booking_confirmed_heading": "تم تأكيد الحجز",
    "view_order": "عرض الطلب"
  },
  "order": {
    "status": {
      "pending": "قيد الانتظار",
      "accepted": "مقبول",
      "washer_assigned": "تم تعيين الغسّال",
      "washer_en_route": "الغسّال في الطريق",
      "in_progress": "جاري التنفيذ",
      "completed": "مكتمل",
      "cancelled": "ملغي",
      "pickup_scheduled": "تم جدولة الاستلام",
      "picked_up": "تم الاستلام",
      "in_cleaning": "قيد التنظيف",
      "ready_for_return": "جاهز للإرجاع",
      "return_scheduled": "تم جدولة الإرجاع",
      "out_for_return": "في طريق الإرجاع",
      "returned": "تم الإرجاع"
    },
    "cancel_order": "إلغاء الطلب",
    "cancel_confirm_heading": "إلغاء طلبك؟",
    "cancel_confirm_body": "الإلغاء مجاني قبل تعيين الغسّال. قد يتم تطبيق رسوم بعد ذلك.",
    "cancel_confirm_cta": "نعم، إلغاء الطلب",
    "order_number": "طلب #{{number}}"
  },
  "dashboard": {
    "order_feed_title": "الطلبات",
    "filter_all": "الكل",
    "filter_pending": "قيد الانتظار",
    "filter_active": "نشط",
    "filter_completed": "مكتمل",
    "assign_washer": "تعيين غسّال",
    "confirm_assignment": "تأكيد",
    "no_orders_heading": "لا توجد طلبات بعد",
    "no_orders_body": "ستظهر الحجوزات الجديدة هنا في الوقت الفعلي.",
    "save_continue": "حفظ ومتابعة",
    "onboarding_profile": "الملف الشخصي",
    "onboarding_services": "الخدمات",
    "onboarding_packages": "الباقات",
    "onboarding_stripe": "ربط Stripe",
    "connect_stripe": "ربط حساب Stripe",
    "stripe_connected": "تم ربط Stripe",
    "add_package": "إضافة باقة",
    "delete_package_confirm": "حذف هذه الباقة؟ لا يمكن التراجع عن هذا.",
    "delete_package_cta": "حذف الباقة",
    "add_washer": "إضافة غسّال",
    "deactivate_washer_confirm": "تعطيل {{name}}؟ لن يتلقى مهام جديدة.",
    "deactivate_washer_cta": "تعطيل"
  }
}
```

Ensure both JSON files are valid after merge. Preserve all existing auth namespace keys.
  </action>
  <verify>
    <automated>cd packages/types && pnpm tsc --noEmit && cd ../../packages/i18n && node -e "const en=require('./locales/en.json'); const ar=require('./locales/ar.json'); console.log('EN keys:', Object.keys(en)); console.log('AR keys:', Object.keys(ar)); if(!en.discovery || !en.booking || !en.order || !en.dashboard) throw new Error('Missing EN namespace'); if(!ar.discovery || !ar.booking || !ar.order || !ar.dashboard) throw new Error('Missing AR namespace')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/types/src/discovery.ts contains `export const companyListQuerySchema`
    - packages/types/src/discovery.ts contains `export const companyProfileResponseSchema`
    - packages/types/src/booking.ts contains `export const createOnSiteOrderSchema`
    - packages/types/src/booking.ts contains `export const createCarpetOrderSchema`
    - packages/types/src/booking.ts contains `export const assignWasherSchema`
    - packages/types/src/company.ts contains `export const companyProfileSchema`
    - packages/types/src/company.ts contains `export const packageSchema`
    - packages/types/src/company.ts contains `export const washerSchema`
    - packages/types/src/index.ts contains `export * from './discovery`
    - packages/types/src/index.ts contains `export * from './booking`
    - packages/types/src/index.ts contains `export * from './company`
    - packages/i18n/locales/en.json contains `"discovery":`
    - packages/i18n/locales/en.json contains `"booking":`
    - packages/i18n/locales/en.json contains `"order":`
    - packages/i18n/locales/en.json contains `"dashboard":`
    - packages/i18n/locales/ar.json contains `"discovery":`
    - packages/i18n/locales/ar.json contains `"booking":`
    - packages/i18n/locales/ar.json contains `"order":`
    - packages/i18n/locales/ar.json contains `"dashboard":`
    - `pnpm tsc --noEmit` in packages/types exits 0
  </acceptance_criteria>
  <done>All Phase 2 Zod schemas exported from @cleanly/types. All i18n keys in both en.json and ar.json with discovery, booking, order, and dashboard namespaces.</done>
</task>

</tasks>

<verification>
- `pnpm prisma validate` passes in packages/db
- `pnpm tsc --noEmit` passes in packages/types
- en.json and ar.json both contain discovery, booking, order, dashboard namespaces
- index.ts barrel exports include discovery, booking, company modules
</verification>

<success_criteria>
- Prisma schema has avg_rating, review_count, carpet_lead_time_days on Company model
- Migration applied to database
- 3 new type files (discovery.ts, booking.ts, company.ts) with Zod schemas
- All schemas exported from @cleanly/types barrel
- i18n keys complete for all Phase 2 screens in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-business-flow/02-01-SUMMARY.md`
</output>
