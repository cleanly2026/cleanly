---
phase: 09-infrastructure-deployment
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md
  - apps/customer-web/.vercelignore
  - apps/admin-web/.vercelignore
  - apps/company-web/.vercelignore
autonomous: false
requirements:
  - VCL-01
  - VCL-02
  - VCL-03
user_setup:
  - service: vercel
    why: "Host the three web apps on Vercel — customer-web and admin-web are Next.js, company-web is a Vite SPA"
    env_vars:
      - name: VERCEL_TOKEN
        source: "Vercel dashboard → Settings → Tokens → Create Token"
    dashboard_config:
      - task: "Create 3 Vercel projects linked to this GitHub repo with correct Root Directory per app"
        location: "Vercel dashboard → Add New Project → Import Git Repository"
      - task: "For each project, set Framework Preset, Root Directory, Build Command, Output Directory, and Ignored Build Step"
        location: "Vercel project → Settings → Build & Development Settings"
      - task: "For each project, set Environment Variables scoped to Preview (staging) and Production"
        location: "Vercel project → Settings → Environment Variables"

must_haves:
  truths:
    - "Customer-web project deploys successfully on Vercel from the current main branch"
    - "Admin-web project deploys successfully on Vercel from the current main branch"
    - "Company-web (Vite SPA) project deploys successfully on Vercel from the current main branch"
    - "All three apps load in a browser with no console errors against the staging API URL"
    - "`turbo-ignore` prevents Vercel from rebuilding an unchanged web app when only a sibling changes"
    - "Vercel Preview and Production envs are scoped separately — a staging Preview build receives `NEXT_PUBLIC_API_URL=https://cleanly-api-staging.fly.dev` and a Production build receives `NEXT_PUBLIC_API_URL=https://cleanly-api.fly.dev`"
  artifacts:
    - path: ".planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md"
      provides: "Exact Vercel project settings, env var tables, deploy commands, rollback steps"
      contains: "Root Directory"
  key_links:
    - from: "Vercel project cleanly-customer-*"
      to: "apps/customer-web"
      via: "Root Directory setting"
      pattern: "apps/customer-web"
    - from: "Vercel project cleanly-admin-*"
      to: "apps/admin-web"
      via: "Root Directory setting"
      pattern: "apps/admin-web"
    - from: "Vercel project cleanly-company-*"
      to: "apps/company-web"
      via: "Root Directory setting, Framework Preset = Vite, Output Directory = dist"
      pattern: "apps/company-web"
---

<objective>
Create three Vercel projects (customer-web, admin-web, company-web) linked to the monorepo, configure each with the correct Root Directory / Framework Preset / Output Directory / Ignored Build Step, populate env vars (scoped to Preview vs Production), deploy, and verify each app loads in a browser without console errors.

Purpose: Get all three web surfaces reachable over HTTPS. The customer and admin apps are Next.js (Output = `.next`). Company-web is a Vite SPA (Framework Preset = Vite, Output = `dist`) — this is the single most error-prone difference.

Output: Three deployed Vercel projects with Preview + Production URLs, env vars populated, `turbo-ignore` set up, runbook documenting every setting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-infrastructure-deployment/09-RESEARCH.md
@.planning/phases/09-infrastructure-deployment/09-01-SUMMARY.md
@turbo.json
@apps/customer-web/package.json
@apps/customer-web/next.config.ts
@apps/admin-web/package.json
@apps/admin-web/next.config.ts
@apps/company-web/package.json
@apps/company-web/vite.config.ts

<interfaces>
<!-- Prerequisites from Plan 01: turbo.json MUST declare NEXT_PUBLIC_* and VITE_* in tasks.build.env -->

Web app package names (confirmed from package.json files):
- `@cleanly/customer-web` — Next.js 15.2.4, Root Directory `apps/customer-web`, Output `.next`
- `@cleanly/admin-web`    — Next.js 15.2.4, Root Directory `apps/admin-web`, Output `.next`
- `@cleanly/company-web`  — Vite SPA, Root Directory `apps/company-web`, Output `dist`

Env vars per app (from Plan 01 turbo.json and each app's source):
- customer-web: `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_SENTRY_DSN`
- admin-web:    `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_SENTRY_DSN`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `ADMIN_EXCHANGE_SECRET`
- company-web:  `VITE_API_URL`, `VITE_SENTRY_DSN`

Preview-scope values (staging):
- `NEXT_PUBLIC_API_URL` / `VITE_API_URL` = `https://cleanly-api-staging.fly.dev`
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` = `pk_test_...`
- `NEXTAUTH_URL` = `https://cleanly-admin-staging.vercel.app`

Production-scope values:
- `NEXT_PUBLIC_API_URL` / `VITE_API_URL` = `https://cleanly-api.fly.dev`
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` = `pk_live_...`
- `NEXTAUTH_URL` = `https://cleanly-admin.vercel.app`

Vercel project URL naming (document convention):
- cleanly-customer-staging.vercel.app + cleanly-customer.vercel.app (2 URLs from ONE project — Preview vs Production git branch)
- Same pattern for cleanly-admin-*.vercel.app and cleanly-company-*.vercel.app

Ignored Build Step command (VCL-01/02/03 explicit requirement):
```
npx turbo-ignore @cleanly/{package-name} --fallback=HEAD^1
```
The `--fallback=HEAD^1` handles first-build-on-branch case.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human creates the three Vercel projects via dashboard or CLI</name>
  <files></files>
  <read_first>
    - .planning/phases/09-infrastructure-deployment/09-RESEARCH.md (section "Vercel project settings" — per-app tables)
    - apps/customer-web/package.json (confirm `@cleanly/customer-web` name)
    - apps/admin-web/package.json (confirm `@cleanly/admin-web` name)
    - apps/company-web/package.json (confirm `@cleanly/company-web` name + vite dependency)
    - apps/company-web/vite.config.ts (confirm build output is `dist`)
  </read_first>
  <what-built>Vercel project creation requires linking to the GitHub repo and clicking through project settings. This is dashboard-only in the happy path. Claude can run `vercel` CLI commands once a human has linked the repo and run `vercel link` per app folder, but the initial project creation + GitHub integration + env var entry is fastest via dashboard.</what-built>
  <how-to-verify>
    Perform these steps in the Vercel dashboard (or via `vercel` CLI equivalents — either works):

    **Project 1 — cleanly-customer-web:**
    1. Vercel dashboard → Add New Project → Import from GitHub → select this repo
    2. Project name: `cleanly-customer-web` (or whatever you prefer — the URL becomes `<name>.vercel.app`)
    3. Framework Preset: **Next.js**
    4. Root Directory: `apps/customer-web`
    5. Build Command: LEAVE BLANK (Vercel auto-detects from Turborepo) — only override if auto-detection fails: `cd ../.. && pnpm turbo run build --filter=@cleanly/customer-web`
    6. Output Directory: `.next` (default — leave blank)
    7. Install Command: LEAVE BLANK (auto)
    8. Node Version: 20.x
    9. Ignored Build Step (Settings → Git → Ignored Build Step): `npx turbo-ignore @cleanly/customer-web --fallback=HEAD^1`
    10. Environment Variables (Settings → Environment Variables). For each variable, select scopes: **Preview** and/or **Production**. Use different values per scope.

       | Variable | Production | Preview (staging) |
       |---|---|---|
       | `NEXT_PUBLIC_API_URL` | `https://cleanly-api.fly.dev` | `https://cleanly-api-staging.fly.dev` |
       | `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | `pk_live_...` | `pk_test_...` |
       | `NEXT_PUBLIC_SENTRY_DSN` | production DSN | staging DSN |

    11. Click Deploy. Wait for the first deploy to complete (success OR failure). Record the production URL.

    **Project 2 — cleanly-admin-web:**
    - Same pattern. Framework Preset: Next.js. Root Directory: `apps/admin-web`.
    - Ignored Build Step: `npx turbo-ignore @cleanly/admin-web --fallback=HEAD^1`
    - Environment Variables (Preview and Production scoped separately):

       | Variable | Production | Preview |
       |---|---|---|
       | `NEXT_PUBLIC_API_URL` | `https://cleanly-api.fly.dev` | `https://cleanly-api-staging.fly.dev` |
       | `NEXT_PUBLIC_SENTRY_DSN` | prod DSN | staging DSN |
       | `NEXTAUTH_SECRET` | random 32+ bytes | different random |
       | `NEXTAUTH_URL` | `https://cleanly-admin.vercel.app` | `https://cleanly-admin-staging.vercel.app` |
       | `GOOGLE_CLIENT_ID` | prod OAuth client | staging OAuth client (or same) |
       | `GOOGLE_CLIENT_SECRET` | prod secret | staging secret |
       | `ADMIN_EXCHANGE_SECRET` | ≥16 chars, MUST match Fly's production secret for cleanly-api | ≥16 chars, MUST match Fly's staging secret for cleanly-api-staging |

    **Project 3 — cleanly-company-web (Vite — DIFFERENT from the Next.js apps):**
    - Framework Preset: **Vite** (NOT Next.js)
    - Root Directory: `apps/company-web`
    - Output Directory: `dist` (THIS IS THE DIFFERENCE — Vite outputs to `dist`, not `.next`)
    - Build Command: usually auto-detected. If auto fails: `cd ../.. && pnpm turbo run build --filter=@cleanly/company-web`
    - Ignored Build Step: `npx turbo-ignore @cleanly/company-web --fallback=HEAD^1`
    - Environment Variables:

       | Variable | Production | Preview |
       |---|---|---|
       | `VITE_API_URL` | `https://cleanly-api.fly.dev` | `https://cleanly-api-staging.fly.dev` |
       | `VITE_SENTRY_DSN` | prod DSN | staging DSN |

    **After creating all three projects:**
    Reply with:
    - "projects created" + the three production URLs and three preview URLs (6 URLs total), OR
    - "failed: {project-name} {error details}"
  </how-to-verify>
  <acceptance_criteria>
    - Three Vercel projects exist: cleanly-customer-web (or equivalent), cleanly-admin-web, cleanly-company-web
    - Each project has Root Directory set correctly (`apps/customer-web`, `apps/admin-web`, `apps/company-web`)
    - Customer and admin use Framework Preset = Next.js; company uses Framework Preset = Vite
    - Company-web Output Directory = `dist`
    - Each project has `npx turbo-ignore @cleanly/<name> --fallback=HEAD^1` in Ignored Build Step
    - Each project has env vars set with separate Preview and Production values per the tables above
    - First deploy has been triggered (may have failed — that's fine, Task 2 debugs)
    - Human provided 6 URLs in the resume signal (production + preview for each)
  </acceptance_criteria>
  <resume-signal>Reply "projects created" with 6 URLs, or "failed: {details}"</resume-signal>
  <action>
[CHECKPOINT TASK — see <what-built> and <how-to-verify> below for the human-driven steps. The executor's role is to present this checkpoint to the user, wait for the resume-signal, and then proceed. All concrete commands and acceptance criteria are specified in <how-to-verify> and <acceptance_criteria>.]
  </action>
  <verify>
    <automated>echo "checkpoint — human verification via resume-signal; see acceptance_criteria"</automated>
  </verify>
  <done>Human replied with the resume-signal indicating all acceptance_criteria are met.</done>
</task>

<task type="auto">
  <name>Task 2: Add .vercelignore per app, trigger redeploys, verify each build</name>
  <files>apps/customer-web/.vercelignore, apps/admin-web/.vercelignore, apps/company-web/.vercelignore, .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/09-infrastructure-deployment/09-RESEARCH.md (section "Vercel project settings")
    - apps/customer-web/next.config.ts (confirm no custom output path)
    - apps/admin-web/next.config.ts (confirm no custom output path)
    - apps/company-web/vite.config.ts (confirm build.outDir defaults to `dist`)
    - apps/customer-web/package.json (confirm the `build` script exists — turbo invokes it)
  </read_first>
  <action>
Three files + runbook.

**1. Create `.vercelignore` at each app root** — minimal file. Vercel's default ignores `node_modules` etc., but we want to exclude .planning, .claude, and test artifacts from the build upload context. Same content for all three:

```
.planning
.claude
**/*.test.ts
**/*.test.tsx
**/__tests__/**
coverage
.turbo
```

Write this identical content to all three files: `apps/customer-web/.vercelignore`, `apps/admin-web/.vercelignore`, `apps/company-web/.vercelignore`.

**2. Trigger a rebuild on each project.** You have two options:

Option A (preferred — git-driven): Commit Plan 01's changes and push. Vercel auto-deploys the branch.

Option B (CLI, if git-push not possible yet): For each app, `cd` into the app directory, run `vercel --prod` for production or `vercel` for preview. This requires the user to have run `vercel link` against each project first.

Execute via whichever is available. Capture the deploy URL for each project.

**3. Verify each deploy succeeds.**

For each of the 6 URLs (3 prod + 3 preview):
```bash
curl -fsS -o /dev/null -w "%{http_code}\n" <URL>
```
Expected: 200 OR 307/308 (redirects are fine — Next.js may redirect to a locale).

For each preview URL, confirm the HTML includes the expected API URL by fetching and grepping:
```bash
curl -s https://cleanly-customer-staging.vercel.app | grep -o "cleanly-api-staging.fly.dev" | head -1
```
Expected: at least one match, proving `NEXT_PUBLIC_API_URL` was correctly inlined.

Do the same for production (`cleanly-api.fly.dev` should appear in the prod HTML).

If staging HTML contains `cleanly-api.fly.dev` (without `-staging`), Turbo cache poisoning occurred — go back to Plan 01 Task 3 and verify turbo.json declares `NEXT_PUBLIC_API_URL` under `tasks.build.env`.

**4. Write the runbook `.planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md`:**

Include:
- Per-project settings tables (Root Directory, Framework Preset, Build Command, Output Directory, Ignored Build Step)
- Per-project env var tables (Preview and Production scoped) — document KEY NAMES ONLY, NEVER values
- Per-project deploy URL (prod + preview)
- Cache-poisoning verification: the grep results for `cleanly-api-staging.fly.dev` in preview HTML and `cleanly-api.fly.dev` in prod HTML
- Rollback:
  ```
  vercel rollback <deployment-url> --scope <team> --yes
  ```
  OR in dashboard: Project → Deployments → find prior deployment → "Promote to Production"
- How to trigger a manual rebuild: `vercel --prod --force` from the app directory, OR dashboard → Deployments → Redeploy
- Note: Vite vs Next.js — the ONE difference that breaks Vercel projects is wrong Framework Preset on company-web. Document loudly.
  </action>
  <verify>
    <automated>test -f apps/customer-web/.vercelignore && test -f apps/admin-web/.vercelignore && test -f apps/company-web/.vercelignore && test -f .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md && grep -q "Root Directory" .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md && grep -q "Framework Preset" .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md && grep -q "turbo-ignore" .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md && grep -q "Vite" .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - Three `.vercelignore` files exist at the app roots, each containing `.planning` and `__tests__`
    - All three Vercel projects completed a successful build (check dashboard → Deployments → Status = Ready)
    - `curl -fsS <prod-url>` returns 200 or 3xx for all three production URLs
    - `curl -fsS <preview-url>` returns 200 or 3xx for all three preview URLs
    - Preview HTML for customer-web contains the string `cleanly-api-staging.fly.dev` (proves NEXT_PUBLIC_API_URL was inlined from the Preview-scoped env var)
    - Production HTML for customer-web contains the string `cleanly-api.fly.dev` (proves correct scope mapping)
    - Runbook exists at `.planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md` with sections: Settings tables, Env var tables (names only), URLs, Rollback procedure, Note on Vite vs Next.js difference
    - Runbook contains no secret values (only key names)
  </acceptance_criteria>
  <done>All 6 Vercel URLs reachable, cache poisoning check passes, runbook documents every setting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human opens each deployed web app in a browser and confirms it loads cleanly</name>
  <files></files>
  <read_first>
    - .planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md (for the URLs to test)
  </read_first>
  <what-built>All three Vercel projects are deployed and their HTML sources reference the correct API URL per scope. Automation cannot confirm the apps actually RENDER without errors in a real browser — a hydration error, missing component, RTL layout bug, or console error is invisible to curl.</what-built>
  <how-to-verify>
    Open each of the 6 URLs in a browser with DevTools Console open.

    For each URL:
    1. Page loads without an error screen (no "Application error" overlay, no blank white page)
    2. DevTools Console shows NO red errors on initial load
    3. DevTools Network tab shows the page fetching from the correct API URL (grep the Network tab for `fly.dev`):
       - Preview URLs should show requests to `cleanly-api-staging.fly.dev`
       - Production URLs should show requests to `cleanly-api.fly.dev`
    4. For customer-web and admin-web (Next.js): navigate to any second route (e.g., `/ar`, `/en`, `/login`) and confirm client-side navigation works
    5. For company-web (Vite SPA): confirm the app bootstraps (React mounts) — no "failed to fetch module" errors

    Known-acceptable warnings (ignore):
    - Sentry DSN not configured (if you haven't set staging Sentry projects yet)
    - "React DevTools recommended" (standard dev tools console note)
    - 401/403 from API if the page requires auth (as long as the page itself renders)

    Critical failures (STOP and report):
    - "Application error: a client-side exception has occurred" — Next.js hydration error
    - 404 on any root URL
    - Blank white page with no console output — SSR crash
    - CORS errors from the API (means ALLOWED_ORIGINS isn't set on Fly — Plan 04 will fix)

    Note on CORS: some API calls WILL fail with CORS errors at this point — that's expected because Plan 04 hasn't wired the Vercel origins into Fly yet. "Cannot reach API due to CORS" is NOT a failure of Plan 03 — it's the reason Plan 04 exists. The PAGE should still render.

    Reply with:
    - "verified" + any non-critical notes, OR
    - "failed: {URL} {console error}"
  </how-to-verify>
  <acceptance_criteria>
    - All 6 URLs render without "Application error" or blank white page
    - No red console errors related to JS loading, hydration, or module resolution
    - Preview URLs attempt API requests to `cleanly-api-staging.fly.dev` (visible in Network tab)
    - Production URLs attempt API requests to `cleanly-api.fly.dev` (visible in Network tab)
    - Human replied "verified" (CORS errors are acceptable; rendering failures are not)
  </acceptance_criteria>
  <resume-signal>Reply "verified" or "failed: {details}"</resume-signal>
  <action>
[CHECKPOINT TASK — see <what-built> and <how-to-verify> below for the human-driven steps. The executor's role is to present this checkpoint to the user, wait for the resume-signal, and then proceed. All concrete commands and acceptance criteria are specified in <how-to-verify> and <acceptance_criteria>.]
  </action>
  <verify>
    <automated>echo "checkpoint — human verification via resume-signal; see acceptance_criteria"</automated>
  </verify>
  <done>Human replied with the resume-signal indicating all acceptance_criteria are met.</done>
</task>

</tasks>

<verification>
Plan 03 is green when:
1. All 6 Vercel URLs (3 prod + 3 preview) return 200/3xx on `curl`
2. Preview HTML contains `cleanly-api-staging.fly.dev`
3. Production HTML contains `cleanly-api.fly.dev`
4. All three apps render in a real browser without JS errors
5. `.planning/phases/09-infrastructure-deployment/09-VERCEL-RUNBOOK.md` documents every project setting
</verification>

<success_criteria>
Maps to phase success criteria:
- Criterion 3 (All 3 web apps load on Vercel without build errors) — Task 1 creates projects, Task 2 triggers builds + verifies 200, Task 3 human confirms render
</success_criteria>

<output>
After completion, create `.planning/phases/09-infrastructure-deployment/09-03-SUMMARY.md` documenting:
- The 6 URLs (3 production + 3 preview)
- The `.vercelignore` file content applied to all three apps
- Cache-poisoning check results
- Runbook path
- Note for Plan 04: "Provide these 6 URLs to Plan 04 which will add them to Fly's CORS allowlist secrets"
</output>
